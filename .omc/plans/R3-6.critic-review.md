# R3-6 Critic Review — DELIBERATE Consensus Pass (iter 2)

**Reviewer:** Critic
**Plan reviewed:** `.omc/plans/R3-6.md` (iter 2)
**Architect review referenced:** `.omc/plans/R3-6.architect-review.md` (iter 2)
**Mode:** THOROUGH — focused grep-verification, no escalation needed (0 CRITICAL, 2 MINOR).

## VERDICT: **APPROVE**

iter 1 六條 ITERATE 條件（A-F）與 Architect R1-R7 十三項整合全數以實質內容落地，非掛名。iter 2 併未引入 principle violation、AC 編號跳號、rollback 閾值漂移或 scope 殘渣；Principle 6 正為 cone-光延後 R3-7 之獨立新原則。一條執行端邊緣事實核對（plan L112 七個 `bounceIsSpecular=FALSE` 行號於 R3-5b shader 實際有 17 處）屬 executor pre-flight 可吸收之 MINOR，不破 iter 2 plan 骨架。

---

## Grep-verification 彙總表

| 檢核項 | 來源 demand | iter 2 落地位置 | 判定 |
|---|---|---|---|
| A — `uCeilingEnabled` a2 (ceiling 排除 Principle 3) | Critic iter 1 C2 | L9 Principle 3 末段「Ceiling quadLight 例外... always-on 主燈... 不納入三源 gate 規則」；L161 AC-M6「無 `uCeilingEnabled`（grep 確認不存在）」；L185 ADR Consequences「R3-8 補 pathway」；shader grep 確認 `uCeilingEnabled` 不存在 | PASS |
| B — S4 cone-leak pre-mortem + 絕對閾值 + grep 診斷 | Critic iter 1 M7 | L63-67 完整（症狀 Cam 2 仰角 Lv > baseline × 1.1；診斷 `grep "hitType == TRACK_LIGHT"` BSDF-indirect 無 `+= mask * emission * wBsdf`；防護 Principle 6）；AC-V4 L169 同步守門 | PASS |
| C — 乙案 ADR honest rewrite (509× / Veach Fig 9.3 / 40-70% MSE) | Critic iter 1 M6 | L34 Viable Options 內誠實記、L177 ADR Alternatives 明載 509× span + 40-70%；附錄 B L222-233 表格自證 ceiling p_ω 0.0631 vs Track 32.1；early-exit L188 | PASS |
| D — AC-V1 FWHM ≤ 2× baseline 或 reference screenshot binary fallback | Critic iter 1 M5 | L17 Driver 2、L85 E2、L148 File Changes 6 新檔 `.omc/plans/R3-6.ref.png`、L166 AC-V1 operationalized | PASS |
| E — per-light-class observability (ceilingAvgW* / cloudAvgW*) | Critic iter 1 E | L90-99 O1 輸出格式拆 ceilingAvgWNee/Bsdf 與 cloudAvgWNee/Bsdf；L165 AC-N4 同步 | PASS |
| F — rollback `?v=r3-6-rollback` cache-buster + Cmd+Shift+R | Critic iter 1 F | L156 AC-M5 明載 `?v=r3-6-rollback` + Cmd+Shift+R 硬重載；L196 Rollback 段同步 | PASS |
| R1 — scope 5 DIFF (ceiling + Cloud)，Track/Wide 延後 R3-7 | Architect R1 | L3 Policy、L7 Principle 1、L109-110 File Changes「維持 R3-5b 行為」、L190 Follow-ups R3-7 | PASS |
| R2 — early-exit (AC-V1 fail → 乙案) | Architect R2 | L36、L188 ADR Early-exit 專段 | PASS |
| R3 — AC-M6 三源 gate grep assertion | Architect R3 | L157-161 四條 grep count（Cloud ≥ 3、Track ≥ 2、Wide ≥ 2、無 Ceiling） | PASS |
| R4 — SPEC state-clear rule | Architect R4 | L112 行號列表；I4 L81 runtime 驗證 | CONDITIONAL — 行號列表不完整（見 MINOR m1） |
| R5 — pixel diff < 2/255, 99th + mean abs < 0.3/255 | Architect R5 | L11 Principle 5、L156 AC-M5 數字完全一致 | PASS |
| R6 — ADR 記 `p*p` vs `pow()` | Architect R6 | L174 ADR 首段明載「Mesa/Apple-M fp32 `pow(x, 2.0)` 走 `exp2(log2(x)*2)` ~1 ULP；`p*p` 單指令 fma <0.5 ULP」 | PASS |
| R7 — `uR3MisPickMode` forward-compat hook | Architect R7 | L134 shader 空殼、L137 JS uniform、L140 GUI 不露、L182 ADR Consequences 納 | CONDITIONAL — 缺 accept vs defer 明載理由（見 MINOR m2） |
| Principle 6 (Track/Wide 延後) 獨立新原則 | — | L12 Principle 6 獨立段，明記 C1 成案機轉 | PASS |
| AC 編號連續 (M1-M6 / N1-N4 / V1-V4 / S1) | — | 15 ACs 無跳號 | PASS |
| scope 縮減後 Principle 1-3 語意一致 | — | Principle 1 末句「Scope：5 DIFF-emitters」；Principle 3 ceiling 例外；無殘留「11 盞皆套」字眼 | PASS |
| rollback AC-M5 閾值與 Architect R5 對齊 | — | pixel diff < 2/255 (99th) + mean abs < 0.3/255 完全吻合 | PASS |

**15/17 PASS + 2 CONDITIONAL（皆 MINOR 不阻 APPROVE）。**

---

## MINOR Findings

### m1. plan L112 state-clear 行號枚舉不完整（非 blocker）

**Evidence**: Plan L112 列 7 處 `bounceIsSpecular = FALSE`（L929/L970/L1007/L1041/L1060/L1110/L1141）。實測 `grep "bounceIsSpecular\s*=\s*FALSE" Home_Studio_Fragment.glsl` 返 **17 處**：L885 / L929 / L970 / L1007 / L1041 / L1060 / L1110 / L1141 / L1183 / L1227 / L1328 / L1360 / L1414 / L1452 / L1500 / L1522 / L1566。Plan 行號為 LIGHT / TRACK_LIGHT / TRACK_WIDE_LIGHT / CLOUD_LIGHT / sampleLight catch-all / SPEAKER DIFF / WOOD_DOOR DIFF 共 7 emitter 分支末之 reset，但漏列 10 處 DIFF-material 分支（IRON_DOOR fallback L1183、BACKDROP L1227、ACOUSTIC_PANEL 類 L1328/L1360/L1414/L1452、CLOUD DIFF sub-branch L1500/L1522、OUTLET L1566、TRACK_HOUSING L885）。
**Why MINOR not MAJOR**: 此類漏列之 DIFF 分支於緊接之後會 dispatch `sampleStochasticLight11` NEE 並立即覆寫 `misWPrimaryNeeLast`（plan L113），state-clear 屬冗餘安全。真正風險僅於 SPEC→DIFF 轉換而未發 NEE 之極小窗口。I4 integration test L81 runtime 驗證（注入 `misWPrimaryNeeLast = 42.0` at bounce 1）足以於 CI 階段抓漏。
**建議 executor pre-flight**: 動工前補 `grep "bounceIsSpecular\s*=\s*FALSE"` 真值 + 逐行判斷是否緊鄰 NEE dispatch，紅線僅 SPEC→DIFF-no-NEE 類。不作 blocker。

### m2. R7 `uR3MisPickMode` 接納理由未明載（非 blocker）

**Evidence**: Plan L134 標「R7 stretch goal，**建議加**」暗示 optional，但 L137/L140/L182 實際落地為必建 uniform + ADR Consequences 條目，非 optional。Planner 未明載「接納 vs 延後」之 trade-off 說明，Critic iter 1 R7 標 optional。
**Why MINOR**: 實作代價極低（1 float uniform + 1 shader 空殼分支），forward-compat 收益（R3-7 升乙案不破簽名）確實合理。但 plan 應明記此 trade-off 判斷而非混用「建議加」與實際必建兩種語氣。
**建議**: executor 若照 plan 落地無須中斷；future plan 遇 stretch goal 時統一「accept」或「defer」而非中間態。

---

## AC-V1 ref.png 生成時機（N4 推進）

Architect N4 提出「`.omc/plans/R3-6.ref.png` 為 File Changes 新檔，Planner 需於 R3-6 動工前 capture」。Plan L148 列為 File Change 6 但未綁 AC 確認此檔於 executor 啟動前已存在。**本 Critic 補一條執行端守門建議**（非 blocker，可由 executor kick-off SOP 承接）：

- **Suggested AC-S0**（executor pre-flight）：`.omc/plans/R3-6.ref.png` 存在於動工前 commit；檔案為 Cam 2 876 spp R3-5b baseline（commit 4f335c5）喇叭面板特寫。若此檔未產，AC-V1 binary fallback 無參照即退化為純主觀。

此為「suggest」級別，不阻 APPROVE；executor /ultrawork 啟動時建議先執行 `capture ref.png → commit → 再開 R3-6 改動`。

---

## What's Missing（剩餘）

- 無結構級缺口。
- 兩 MINOR m1/m2 可由 executor pre-flight + CI I4 test 吸收。
- ref.png 產出時機屬工序，非 plan 缺漏。

---

## Ralplan Summary Row (iter 2)

| Gate | Status | 理由 |
|------|--------|------|
| Principle/Option Consistency | **Pass** | Principle 1-3 scope 5 DIFF 一致；Principle 6 新增明記 Track/Wide 延後；ceiling 例外於 ADR Consequences 完整 |
| Alternatives Depth | **Pass** | 乙案附錄 B 自證 509× span、projected MSE 40-70%；early-exit path 明定乙為 canonical fallback |
| Risk/Verification Rigor | **Pass** | S1-S4 + AC-M1~M6 + U4 + I4 + O1 per-light-class 全備；AC-V1 operationalized |
| Deliberate Additions (pre-mortem + expanded test) | **Pass** | 4 scenarios 含絕對閾值；Unit/Integration/E2E/Observability 四層俱全 |

四 gate 全 Pass → APPROVE confirmed。

---

## Verdict Justification

iter 1 六條 ITERATE 條件（A-F）+ Architect R1-R7（含 R7 stretch）全數以實質內容整合，非掛名。scope 縮 11 → 5 盞之後 Principle 1-3 語意乾淨、Principle 6 獨立新增、ADR 乙案 early-exit 完整、AC-M6 gate grep assertion 與 shader 真實狀態（無 `uCeilingEnabled`）對齊。rollback 閾值與 Architect R5 字字一致。

兩條 MINOR（plan L112 state-clear 行號漏列、R7 accept/defer 語氣未統一）皆於 executor pre-flight 與 I4 runtime test 可吸收，不阻 APPROVE。未升至 ADVERSARIAL mode（0 CRITICAL、0 MAJOR）。

**Realist Check**: m1 最壞情況為執行端漏補 state-clear 於某 DIFF 分支，致 SPEC-chain bounce 之 stale `misWPrimaryNeeLast` 讀回。I4 注入測試會於 CI 抓到（plan L81 明定），detection 時間秒級，修復僅補一行 shader reset。不升級。m2 最壞情況為 executor 誤讀「建議加」為 optional 跳過 R7，後續 R3-7 升乙案需破簽名——但 plan 三處（L134/L137/L140）皆直接列為新 uniform，executor 照 plan 落地即會實作，語氣不一致不產功能缺漏。不升級。

---

## Hand-off Notes（為 executor 作）

**推薦模式**：**`/oh-my-claudecode:ultrawork`**

**理由**：
- R3-6 為依賴鏈循序任務（helper → signature → state vars → 4 branches → SPEC state-clear → JS uniforms → GUI → E2E），不可併行。
- 每步須肉眼 / grep 驗收（AC-M1~6 + AC-V1~4 + AC-N1~4 + AC-S1），適合 ultrawork「改 → 驗 → 修 → 再驗」循環。
- Team / ralph 不合：前者需獨立檔案併行，後者過於自由無主幹。

**executor-time gotchas**：
1. **Ref screenshot 先行**：動工前先 `git checkout 4f335c5 → 876 spp capture Cam 2 喇叭特寫 → 存 `.omc/plans/R3-6.ref.png` → commit → 再切回 main`。Plan L148 為 File Change 但未列於 AC 前置。
2. **state-clear 全域 grep**：動工後立即 `grep "bounceIsSpecular\s*=\s*FALSE"` shader 實測 17 處（非 plan L112 列 7 處），逐行判斷是否緊接 NEE dispatch；紅線為 SPEC→DIFF 無 NEE 分支。I4 test 落地前先手動清查。
3. **`sampleStochasticLight11` 10 呼叫點 atomic update**：grep `sampleStochasticLight11\(` 於 shader 應為 10 處；簽名 4→6 args 須一次全改否則 GLSL compile 全掛。AC-M2 守門。
4. **Cone-leak 紅線**：TRACK_LIGHT / TRACK_WIDE_LIGHT BSDF-indirect 分支**不得**出現 `misPowerWeight` 或 `+= mask * emission * wBsdf`。AC-M3 + AC-V4 雙守門。
5. **rollback sanity check**：實作完之後第一件事執行 `uR3MisEnabled.value = 0` 雙驗 AC-M5 `pixel diff < 2/255`。Cmd+Shift+R 硬重載 + `?v=r3-6-rollback-verify` query。若此守門 fail，必是 default 路徑意外動到 R3-5b code path，立即排查非進入 MIS 調參。
6. **Observability 先行**：O1 per-light-class log 於前 3 bounce 即可觀測 avgSum；若 avgSum < 0.9 或 > 1.1 → heuristic 公式錯，別往 visual regression 查。

**進度打卡**：每完成 AC-M1/M2/M3/M4/M5/M6 執行端驗收即於 SOP 內文 + outline 雙標 ✅（`feedback_sop_dual_checkmark.md`）。

**若 AC-V1 fail**：照 plan L188 ADR Early-exit 終止甲案，啟 R3-7 plan 改走乙案 CDF pick（uR3MisPickMode = 1.0）；不進原 R3-7 cone-aware NEE。

Plan 可動工。
