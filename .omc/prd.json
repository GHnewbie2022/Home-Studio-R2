{
  "meta": {
    "title": "R4-1 UI 骨架復刻（HTML panel + CSS + createS + DOM adapter；丟棄 lil-gui；InitCommon.js 改造）",
    "planRef": ".omc/HANDOVER-R4.md",
    "branch": "r3-light",
    "scope": "Atomic switch: port old-project HTML panel skeleton + CSS + createS factory + DOM adapters; remove lil-gui entirely; modify InitCommon.js",
    "workingDir": "/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/",
    "predecessor": "R4-0 DONE (UI inventory + ralplan 2-iteration consensus)",
    "cacheBusterPrefix": "r4-1",
    "riskLevel": "HIGH（atomic switch — page breaks if partial）",
    "userConstraints": [
      "Option A atomic switch: no lil-gui coexistence period",
      "1:1 old-project style replication, not wabi-sabi",
      "No cavity-panel, no post-panel",
      "Port 9001 URL: http://localhost:9001/Home_Studio.html",
      "對使用者嚴禁技術縮寫",
      "每改遞增 cache-buster"
    ]
  },
  "stories": [
    {
      "id": "US-R4-1-S1",
      "title": "CSS extraction: port old-project panel/button/slider/gik styles to css/default.css",
      "priority": 1,
      "passes": true,
      "acceptanceCriteria": [
        "css/default.css 尾端新增所有 panel UI class（約 130 行）：#ui-container, .panel-group, .panel-header, .panel-content, .panel-content.collapsed, .sub-panel, .item-divider, .control-row, .label-group, .label-text",
        "input[type=range], input[type=number], input[type=checkbox] styling 存在",
        "button.action-btn 基色 + hover + 4 glow variants (glow-white, glow-orange, glow-blue, glow-gradient-ob, glow-gradient-bo) 存在",
        ".gik-minimap, .gik-row, .gik-label, .gik-blocks, .gik-block[data-color=0/1/2/3] 存在",
        ".gik-palette, .gik-color-btn 存在",
        ".cam-btn, #help-btn, #hide-btn, #instructions, #help-wrapper:hover #instructions 存在",
        "#snapshot-bar, .snapshot-thumb, .snapshot-label, .snapshot-save, #snapshot-preview, #snapshot-actions, .snapshot-action-btn 存在",
        "#sample-counter, #info-panel styling 存在",
        "#loading-screen 相關 styling 不需（inline style in HTML）",
        "舊 css/default.css 的 #info, #cameraInfo, .toggleButton 等既有樣式不被破壞"
      ]
    },
    {
      "id": "US-R4-1-S2",
      "title": "HTML skeleton: remove lil-gui refs, add #ui-container 3 panel-groups + fixed controls",
      "priority": 2,
      "passes": true,
      "acceptanceCriteria": [
        "Home_Studio.html <style> 區塊移除 L10-14 四行 .lil-gui 巢狀覆寫",
        "Home_Studio.html <script type=module> 移除 import GUI + window.GUI = GUI 兩行",
        "<body> 內新增 <div id=\"ui-container\"> 含一個 .panel-group > 3 header+panel 區塊：⚙️光追設定(#ray-header/#ray-panel) + 🔲材質設定(#mat-header/#mat-panel) + 💡燈光設定(#light-header/#light-panel)",
        "#ray-panel 含：A/B radio buttons (#btnGroupA/#btnGroupB) + #slider-bounces + #group-a-controls(#slider-clamp-a, #slider-mult-a) + #group-b-controls(#slider-clamp-b, #slider-mult-b) + pixel_Resolution slider (#slider-pixel-res)",
        "#mat-panel 含：#slider-wall-albedo + #groupGikPresets(2 buttons) + .gik-minimap(5 row, 含 all/ceil/北牆3/東牆3/西牆3)",
        "#light-panel 含：#slider-emissive-clamp + Cloud 區塊(#chkCloud + #groupCloudColor 3btn + #slider-lumens) + Track 區塊(#chkTrack + #groupTrackMeshColor 2btn + #groupTrackColor 4btn + 6 slider div) + Wide 區塊(#chkTrackWideSouth/#chkTrackWideNorth + #groupTrackWideMeshColor 2btn + #groupTrackWideColorSouth/#North 各3btn + 5 slider div)",
        "新增 #top-right-group 含 #btnCam1/#btnCam2/#btnCam3",
        "新增 #bottom-right-group 含 #help-wrapper(#help-btn + #instructions) + #hide-btn",
        "新增 #info-panel 含 #fps-counter + #sample-counter",
        "新增 #loading-screen + #loading-ring + #loading-text (SVG 圓環)",
        "#snapshot-bar 保留（已存在）",
        "不含 #cavity-panel, #post-panel, #post-header, #cavity-header",
        "<script defer src=\"js/lil-gui.module.min.js\"> 不存在（之前就沒有 script tag，是 import 在 module block）",
        "cache-buster 更新 Home_Studio.js?v=r4-1-skeleton"
      ]
    },
    {
      "id": "US-R4-1-S3",
      "title": "createS factory + DOM adapters + panel toggle logic in Home_Studio.js",
      "priority": 3,
      "passes": true,
      "acceptanceCriteria": [
        "js/Home_Studio.js 新增 function createS(id, label, min, max, step, init, cb, reset=true) — 搬入舊專案 L2083-2089 邏輯：innerHTML 建 .control-row + range + number + meta+click reset",
        "createS 內建 meta+click reset（metaKey||ctrlKey + button===0 → 回 init）",
        "createS 的 cb 回傳值若 !== undefined 則覆蓋顯示值（用於 clamp 邏輯）",
        "新增 DOM adapter: setSliderValue(sliderId, value) — 寫入 #sliderId 內 range + number input",
        "新增 DOM adapter: getSliderValue(sliderId) — 讀取 #sliderId 內 range input value 回傳 parseFloat",
        "新增 DOM adapter: setSliderEnabled(sliderId, enabled) — toggle pointer-events:none + opacity:0.4 on container",
        "新增 DOM adapter: setSliderLabel(sliderId, label) — 更新 #sliderId 內 .label-text textContent",
        "新增 DOM adapter: setCheckboxChecked(checkboxId, checked) — 同步 checkbox.checked",
        "新增 panel-header toggle 邏輯：每個 .panel-header click → 對應 .panel-content toggle .collapsed class"
      ]
    },
    {
      "id": "US-R4-1-S4",
      "title": "Refactor setupGUI → initUI: replace all lil-gui calls with createS + DOM adapter",
      "priority": 4,
      "passes": true,
      "acceptanceCriteria": [
        "function setupGUI() 更名為 function initUI() 或內容完全重寫移除所有 gui.add / gui.addFolder 呼叫",
        "所有 lil-gui controller 變數（samplesPerFrameController, brightnessCtrl, trackLightCtrl, wideTrackLightCtrl, cloudLightCtrl, r3DynamicCtrl, activeLightCountDisplay, cloudColorCtrl, trackColorCtrl, trackLumensCtrl, trackWideColorSouthCtrl, trackWideColorNorthCtrl, trackWideLumensCtrl）改為 DOM-based 實作或 adapter 包裝",
        "brightnessCtrl.setValue / .disable() / .enable() / .name() 全改為 setSliderValue / setSliderEnabled / setSliderLabel 呼叫",
        "trackLightCtrl.updateDisplay() 改為 setCheckboxChecked('chkTrack', value)",
        "wideTrackLightCtrl.updateDisplay() 改為 setCheckboxChecked 呼叫",
        "cloudLightCtrl.updateDisplay() 改為 setCheckboxChecked('chkCloud', value)",
        "applyPanelConfig 內所有 lil-gui API（.setValue/.disable/.enable/.name/.updateDisplay）改為 DOM adapter",
        "syncR3ColorUIEnable 改為操作 DOM（setSliderEnabled 或直接 pointer-events toggle）",
        "gui.domElement.addEventListener('click', e => e.stopPropagation()) 改為 document.getElementById('ui-container').addEventListener",
        "呼叫點 setupGUI() 改為 initUI()",
        "attachMetaClickReset 全部移除（createS 內建）",
        "camPos / applyCamManual / cam 5 sliders 移除（Cam 按鈕在 R4-5 接線，此處只建骨架 button）"
      ]
    },
    {
      "id": "US-R4-1-S5",
      "title": "InitCommon.js transformation: remove GUI, port pixel_Resolution, rebind pointer-lock",
      "priority": 5,
      "passes": true,
      "acceptanceCriteria": [
        "InitCommon.js L439 gui = new GUI() 移除",
        "L441-442 gui.domElement.style 兩行移除",
        "_attachMetaClickReset 函式整段移除（L446-458）",
        "pixel_ResolutionController = gui.add(...) 改為透過 createS('slider-pixel-res', ...) 建構（呼叫 Home_Studio.js 的 createS）",
        "L462-472 gui.domElement mouseenter/mouseleave → 改綁 document.getElementById('ui-container')",
        "L682 pixel_ResolutionController.setValue(pixelRatio) → setSliderValue('slider-pixel-res', pixelRatio)",
        "L931 pixel_ResolutionController.getValue() → getSliderValue('slider-pixel-res')",
        "全域變數 gui 不再被 InitCommon.js 使用（grep 'gui' in InitCommon.js 命中僅限注釋或無關變數）"
      ]
    },
    {
      "id": "US-R4-1-S6",
      "title": "Delete lil-gui + integration verification",
      "priority": 6,
      "passes": true,
      "acceptanceCriteria": [
        "js/lil-gui.module.min.js 檔案已刪除",
        "Grep 'lil-gui' across entire project = 0 命中（排除 .git/）",
        "Grep 'new GUI' across js/*.js = 0 命中",
        "Grep 'gui.add\\|gui.addFolder\\|gui.domElement' across js/*.js = 0 命中",
        "頁面 http://localhost:9001/Home_Studio.html 載入無 console error（無 GLSL compile error, 無 import error, 無 TypeError）",
        "3 個 panel-header（⚙️光追設�� / 🔲材質設定 / 💡燈光設定）click 可 toggle collapsed class",
        "pixel_Resolution slider 可拖曳（range input oninput 觸發 handlePixelResolutionChange）",
        "applyPanelConfig(1) 正常執行不拋錯（DOM adapter 運作）",
        "applyPanelConfig(3) 正常執行：吸頂燈 slider disabled + label 改為 CONFIG 3 已拆除",
        "Cam 1 下渲染正常（canvas 非全黑非全白，sampleCount 可累加）",
        "cache-buster = r4-1-skeleton"
      ]
    }
  ],
  "outOfScope": [
    "控件接線（R4-3）：所有 slider onChange 暫接 wakeRender() 空殼",
    "甜蜜點 slider（R4-4）：HTML div 骨架存在但 createS 留空殼",
    "Cam 按鈕接線（R4-5）：button 存在但 onclick 暫不接",
    "Snapshot 邏輯（R4-5）",
    "Loading screen 邏輯（R4-5）",
    "FPS/sample counter update binding（R4-5）",
    "Hide UI toggle（R4-5）",
    "Help hover（R4-5）",
    "shader 分支扁平化（R4-2）",
    "sampleStochasticLight11 deletion（R4-2）"
  ]
}
