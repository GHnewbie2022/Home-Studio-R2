# R3：燈光系統（真光源升級）

---

## 🚨 接手第一步（不得跳過）

**必讀**：`/Users/eajrockmacmini/Documents/VS Code/My Project/SOP/Debug_Log.md` 開頭「通用 Debug 紀律」章節（三條規則 + 驗證紀律）。

R2-14 曾連續三任 Claude 翻車（fix02 幾何、fix03 Z-fighting、fix04 shadow 自遮蔽），皆因跳過此紀律。未讀完勿動 code。

**必讀**：`/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/docs/R3_燈具產品規格.md`。
所有光源 emission 必須以此檔之 lumens / beam angle / 色溫為單一真值來源。禁止回到「brightness=900」抽象單位思維。

---

## 🔧 git 工作流程（R3 改為 branch-per-phase）

R2 以前用資料夾複製快照，R3 起改為 git branch 工作流（業界標準）。

```
專案路徑    ：Home_Studio_3D/（原 Home-Studio-R2/，2026-04-19 改名）
GitHub repo ：github.com/GHnewbie2022/Home_Studio_3D（已改名，舊 URL 自動 redirect）
GitHub Pages：https://ghnewbie2022.github.io/Home_Studio_3D/Home_Studio.html

分支佈局：
  main          ← R2-1 ~ R2-18 完工穩定版（tag: v2.18-final）
  r3-light      ← R3 工作分支（R3-0 ~ R3-8 全部 commit 在此）
  
  R3 全部驗收 ✅ 後再 merge r3-light → main，並 tag v3.0-final

規則：
  - 不得在 main 上 commit R3 的修改，所有 R3 動作 git checkout r3-light 確認再動手
  - 若 R3 某階段出問題要回滾：git checkout -- <file> 或 git reset --hard HEAD~1
  - 若要完全放棄 R3 回到 R2：git checkout main（r3-light 分支保留，隨時可回）
  - 每個 R3-N 完成即 commit，message 格式：「R3-N: 主題概述」
```

本地伺服器 cwd：`/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/`（port 9001）
本地驗證 URL：`http://localhost:9001/Home_Studio.html?v=<cache-buster>`

---

## 階段重排說明（2026-04-19）

R3-3 完工時僅落地 emissive 幾何與量綱修正，原計畫之 3-face area sampling NEE 未實作（Cloud 對其他表面貢獻 = 0，僅剩自發光燈管）。R3-4 完工時 stochastic NEE 基礎建設（sampleStochasticLight5）就緒，反映出 R3-3 階段 scope 砍半造成的視覺缺失需回補。

原 R3-5（廣角燈 + Many-Light + MIS）併包三件事，難度分佈 ★★（廣角燈複用 R3-4 pattern）↔ ★★★★（Cloud NEE area sampling 原創）↔ ★★★（MIS 整合），集中於單階段高風險。

按工程難度重排（2026-04-19 使用者核可）：
- R3-5 拆為 R3-5a（Track wide, ★★）+ R3-5b（Cloud NEE 補漏, ★★★★）
- R3-6 新增（MIS 整合 ★★★，從原 R3-5 Part B/C 拆出）
- 原 R3-6 indirectMul 校準 → R3-7
- 原 R3-7 採購評估 → R3-8

**注意**：shader / JS 程式碼註解中之「R3-5 MIS 到位」「R3-5 承接」等字串為重排前語境，未同步更新（避免 cache-buster / 編譯影響）。讀 code 見此類字串時，以本 SOP 新編號為準：
- shader L1320「R3-5 MIS 到位前避免 double-count」實指 R3-6
- shader L80「供 R3-5 MIS area sampling 承接」實指 R3-5b
- JS L455 / L504 / L990 / L1083 / L1088 內「R3-5」泛指 R3-5a/5b/6 其中之一
- `.omc/plans/r3-3-*.md` / `r3-4-*.md` 為 R3-3/R3-4 當時 ralplan 歷史決策紀錄，內文「R3-5 MIS」「R3-6 indirectMul 歸一」屬撰寫當下之舊編號語境，不同步以保留決策軌跡；以此表為翻譯鍵

---

## 階段總覽

| 階段 | 主題 | 難度 | 狀態 |
|------|------|------|------|
| R3-0 | `weight * 1.5` 魔數抽離為 uniform | ★ | ✅ |
| R3-1 | 光通量 → radiance 單位換算層（JS + shader） | ★★ | ✅ |
| R3-2 | 色溫 → RGB 換算（kelvinToRGB） | ★★ | ✅ |
| R3-3 | Cloud 漫射燈條真光源（emissive 幾何 + 量綱；NEE 漏補留 R3-5b） | ★★ | ✅ |
| R3-4 | 軌道投射燈真光源（4 盞，spot cone NEE） | ★★ | ✅ |
| R3-5a | 軌道廣角燈真光源（2 盞，slot 5-6 複用 R3-4 pattern） | ★★ | ✅ |
| R3-5b | Cloud 漫射燈條 NEE 補漏（4 rod line-emitter，對角 Lambertian + 長軸 jitter + NEE pool gate 對稱） | ★★★★ | ✅ |
| R3-6 | Many-Light Sampling + MIS 整合（power heuristic β=2，11 盞統整） | ★★★ | ✅ |
| R3-7 | indirectMul 校準歸一（max_bounces 4→8，量測） | ★★ | ⬜ |
| R3-8 | 採購評估驗收（4 情境切換測試） | ★ | ⬜ |

---

## 架構決策

**已定案**：沿用 R2-18 現架構延伸（非回去走 GI baking / probe / lightmap 老路）。

```
框架：erichlof three.js-path-tracing-renderer
演算法：unidirectional path tracing + NEE + MIS + Many-Light Sampling
MIS 權重：power heuristic（β=2）
光源選取：stochastic 每次 NEE 均勻選 1 盞，selectPdf 納入 MIS 計算
色溫：每光源獨立（uCloudKelvin、uTrackKelvin[4]、uTrackWideKelvin[2]）
```

**為何不走 BDPT**：使用者舊專案 Demo 5/8 遇到的牆縫漏光、側牆環 artifact 正是 BDPT 在薄牆幾何的典型副作用，不划算。

**為何不用 ReSTIR**：雖為 SOTA（Siggraph 2020），但 erichlof 無現成 demo，要從 paper 從零實作，風險高於收益。

---

## R3-0 魔數 `weight * 1.5` 抽離 ✅

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`

**前置條件**：R2-18 驗收通過 ✅。

**問題背景**：
shader 中 `mask *= weight * 1.5;` 出現 10 處（L837, 868, 909, 954, 1055, 1087, 1139, 1177, 1208, 1255），橫跨所有材質分支。這個 1.5 是 Phase 2 為補償某個全域間接光衰減而手調出來的魔數，與 `uIndirectMultiplier=1.7` 不同層。若 R3-1 直接改光源能量單位而不先抽離，會造成「真光源 radiance × 1.5 魔數 × 1.7 indirectMul」三層複合補償，後續 MIS 上線就無法診斷能量來源。

**做法**：
1. shader 新增 uniform `float uLegacyGain;`
2. 10 處 `mask *= weight * 1.5;` 全改為 `mask *= weight * uLegacyGain;`
3. JS 新增 uniform init：`uLegacyGain = 1.5`（預設值維持現況）
4. **不改邏輯**，只集中管理，為 R3-6 MIS 上線後歸一做準備

**完成條件**：
1. ✅ grep 全檔無 `weight * 1.5` 字樣（shader/JS 零殘留；SOP 文件保留屬歷史紀錄）
2. ✅ 10 處 `mask *= weight * uLegacyGain;` 出現（shader 行號 840/871/912/957/1058/1090/1142/1180/1211/1258）
3. ✅ Cam 1/2/3 × 500 spp 截圖與 R2-18 定案版像素級一致（使用者 2026-04-19 肉眼確認）
4. ✅ 關閉 `uLegacyGain=0` 畫面變暗反向驗證（使用者授權跳過；由像素級一致 + shader compile 通過間接證明 uniform 生效）

**cache-buster**：`r3-0-legacy-gain-uniform`

---

## R3-1 光通量 → radiance 單位換算層 ✅

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`

**前置條件**：R3-0 完成 ✅，R3_燈具產品規格.md 已建立 ✅。

**核心：三個 JS 函式**

函式 1（搬自舊專案 `Path Tracking 260412a 5.4 Clarity.html:1752`）：
```javascript
function lumenToCandela(lm, beamAngleDeg) {
    const halfRad = Math.max(0.01, (beamAngleDeg / 2.0)) * Math.PI / 180.0;
    return lm / (2.0 * Math.PI * (1.0 - Math.cos(halfRad)));
}
```

函式 2（新增，candela → radiance）：
```javascript
function candelaToRadiance(cd, emitterAreaM2) {
    // 假設 emitter 面發光均勻、Lambertian 外放
    // L = I / A（對 spot emitter 近似；真正 Lambertian 需乘 π 但此處以 emitter 面積分攤）
    return cd / Math.max(emitterAreaM2, 1e-6);
}
```

函式 3（新增，photopic 光通量 → 輻射通量）：
```javascript
function lumensToWatts(lm, kelvin) {
    // 依色溫查光視效能 K(T)
    //   3000K: 約 300 lm/W
    //   4000K: 約 320 lm/W
    //   6500K: 約 340 lm/W
    const K = kelvinToLuminousEfficacy(kelvin);  // 新表查
    return lm / K;
}
```

**shader 端 uniform 改動**：

```glsl
// 移除（R2-18 舊）
// uniform vec3 uLightEmission;        // 吸頂燈 radiance（保留，為 Config 1/2 吸頂燈單光源）
// uniform float brightness;           // 無單位標量（R3-4 後退休）

// 新增（R3-1 目標）
uniform vec3 uCloudEmission[4];        // 每根燈條 radiance (W/sr/m²)
uniform vec3 uTrackEmission[4];        // 每盞投射燈 radiance
uniform vec3 uTrackWideEmission[2];    // 每盞廣角燈 radiance
```

**JS 端 uniform 餵值流程**（R3-1 只建管線，不接 shader 發光邏輯；邏輯留給 R3-3/4/5）：

```
GUI 滑桿（lumens 單位）
  → lumensToWatts(lm, kelvin)            [光通量 → 輻射通量]
  → candelaToRadiance(cd, emitterArea)   [spot] 或 直接 W/(area * 2π)  [漫射]
  → kelvinToRGB(kelvin) 做色溫調色
  → 寫入 uCloudEmission / uTrackEmission / uTrackWideEmission uniform
```

**完成條件**：
1. ✅ 四支 JS 換算函式（lumenToCandela / candelaToRadiance / lumensToWatts / kelvinToLuminousEfficacy LUT）在 Home_Studio.js 可被呼叫並通過 unit test（Node 驗算：lumens=2000 / beam=60° 全錐角 → candela ≈ **2375.90**；15° → 37206.86；120° → 636.62。`lumenToCandela` 第二參數為「全錐角」而非半角，docstring 明示以防後續 R3-3/4 誤用）
2. ✅ 三組 emission uniform 陣列（`uCloudEmission[4]` / `uTrackEmission[4]` / `uTrackWideEmission[2]`）+ DCE-proof gate（`uR3EmissionGate` 預設 0.0）已於 shader 宣告並在 JS 端寫入；emission 值一律 0（R3-3/4/5 填入），gate runtime-valued 防 GLSL 編譯器把陣列折除
3. ✅ shader 光路邏輯未改，畫面與 R3-0 基線（= R2-18 定案）像素級一致（2026-04-19 Cam 1/2/3 × 500 spp 使用者肉眼通過）
4. ✅ `brightness` / `colorTemp` GUI slider 已 `.disable()` + `.name('... (R3-6 校準)')`（JS 字串保留原編號，重排後實指 R3-7），onChange 與 attachMetaClickReset 保留（R3-7 重新校準時才重新啟用）

**cache-buster**：`r3-1-fix01-guard-ordering`（初版 `r3-1-lumens-uniform-pipeline` 遇 fix01 bug 已廢；詳見 Debug_Log.md 「R3-1 fix01」）

---

## R3-2 色溫 → RGB 換算 ✅

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`（或抽到 `js/ColorTemperature.js`）

**前置條件**：R3-1 完成 ✅。

**演算法選擇**：Tanner Helland 近似（速度快、誤差 < 5%、對攝影棚色溫範圍 1000K~10000K 適用）。

```javascript
function kelvinToRGB(kelvin) {
    const t = Math.max(1000, Math.min(40000, kelvin)) / 100;
    let r, g, b;

    // Red
    if (t <= 66) r = 255;
    else r = 329.698727446 * Math.pow(t - 60, -0.1332047592);

    // Green
    if (t <= 66) g = 99.4708025861 * Math.log(t) - 161.1195681661;
    else g = 288.1221695283 * Math.pow(t - 60, -0.0755148492);

    // Blue
    if (t >= 66) b = 255;
    else if (t <= 19) b = 0;
    else b = 138.5177312231 * Math.log(t - 10) - 305.0447927307;

    return [
        Math.max(0, Math.min(255, r)) / 255,
        Math.max(0, Math.min(255, g)) / 255,
        Math.max(0, Math.min(255, b)) / 255
    ];
}
```

**光視效能查表**（lumensToWatts 用）：

```javascript
function kelvinToLuminousEfficacy(kelvin) {
    // 基於 Planck 黑體 × CIE photopic curve 積分（簡表）
    // 可進一步查表或用 1-D 插值
    if (kelvin <= 2700) return 280;
    if (kelvin <= 3000) return 300;
    if (kelvin <= 4000) return 320;
    if (kelvin <= 5000) return 330;
    if (kelvin <= 6500) return 340;
    return 350;
}
```

**驗收測項**：

| 輸入 K | 期望 RGB（Tanner Helland） | 容許誤差 |
|--------|---------------------------|----------|
| 3000 | (1.00, 0.75, 0.42) | ± 0.03 |
| 4000 | (1.00, 0.89, 0.76) | ± 0.03 |
| 6500 | (1.00, 0.99, 1.00) | ± 0.03 |

**完成條件**：
1. ✅ kelvinToRGB 函式完成並通過上表驗收（演算法由原訂 Tanner Helland 近似改為 PCHIP + Mitchell Charity sRGB 查表，5 錨點 2000/3000/4000/6500/10000K，contract-test `docs/tests/r3-2-kelvin.test.js` 25/25 PASS）
2. ✅ R3-1 的 `uCloudEmission[i]` 調色管線保留（fix01 凍結 emission=0，等 R3-3 接手 shader 光路時才啟用 `kelvinToRGB(cloudKelvin)` 寫入；`computeLightEmissions` 註解已標 R3-3 範例）
3. ✅ GUI 色溫控制完成（fix01 起改 per-light-group dropdown preset：Cloud 3 檔 / Track 5 檔 / TrackWide 南北獨立各 3 檔，依商品規格 WARM/NEUTRAL/COLD 離散化；原計畫 slider 改為 dropdown 以對齊商品規格）
4. ✅ shader 光路未動（R3-1 `uR3EmissionGate=0` 凍結維持），GUI dropdown 僅更新 JS 端 state，畫面不受影響
5. ✅ Config 聯動：`applyPanelConfig → syncR3ColorUIEnable` 切換 — Config 1/2 R3 dropdown disable（吸頂燈撐場景）、Config 3 enable
6. ✅ kelvinToLuminousEfficacy：R3-2 不需（原列入完成條件為誤植，實際屬 R3-1 lumens→watts 管線範疇，R3-1 已完成）

**cache-buster 沿革**：
- 主階段：`r3-2-kelvin-rgb` → `r3-2-v2-kelvin-rgb`（fix PCHIP 演算法切換）
- fix01：`r3-2-fix01-ui-preset`（slider → dropdown + Config 聯動）
- fix01b：`r3-2-fix01b-ui-level`（吸頂燈色溫 UI 撤、R3 dropdown 平級化）
- fix01c：`r3-2-fix01c-ui-zh`（Light Settings 全中文 + 標題重排）

**備註（fix01/b/c 改動歷程）**：

| fix | 主題 | commit |
|-----|------|--------|
| fix01 | 色溫 slider → dropdown preset + Config 聯動 | 0721afb |
| fix01 deslop | 合併 3-選單共用 label↔mode 映射（Cloud/南/北 共 WARM3_*） | 71d89b1 |
| fix01b | 吸頂燈色溫 UI 撤除（保留內部 colorTemperature=4000 常數），R3 dropdown 平級化（拆 subfolder） | 5946785 |
| fix01c | 全中文化（吸頂主燈/北廣角燈/南廣角燈/間接光倍率/最大彈跳數）+ 順序重排（北先於南、間接光先於彈跳數）+ brightness 恢復可調 | 846b87f |

UI 最終樣貌（Light Settings 7 項同級、全中文）：
1. 吸頂主燈（brightness 滑桿，0-4000，預設 900）
2. Cloud漫射燈（3 檔 dropdown：暖/自然/冷）
3. 東西軌道燈（5 檔 dropdown：全暖/全自然/全冷/北暖南冷/北冷南暖）
4. 北廣角燈（3 檔 dropdown）
5. 南廣角燈（3 檔 dropdown）
6. 間接光倍率（0.5-3.0 滑桿，預設 1.7）
7. 最大彈跳數（1-14 滑桿，預設 4）

**遺留 DONE-gated 項目（等 R3-3/4/5 階段處理）**：
- Config 3 吸頂燈 emission 歸 0（目前因 R3 其他光源未完工，暫保留吸頂燈 path 撐場景）
- UI 侘寂（wabi-sabi）樣式回歸（R4-UI 階段，參考舊專案 HTML 按鈕排版）

---

## R3-3 Cloud 漫射燈條真光源 ✅（2026-04-19 完工）

**主 commit 待 push。cache-buster `r3-3-fix03-inner-face-skip`**。

落地摘要：
1. JS `computeCloudRadiance(lm, T_K, A) = (lm/3)/(K(T)·π·A)` + `CLOUD_ROD_LUMENS[1152,1152,848.64,848.64]` + `CLOUD_ROD_FACE_AREA[0.0384,0.0384,0.02829,0.02829]` + runtime assertion `CLOUD_BOX_IDX_BASE = 71`（**非註解邏輯 ID 55，見 feedback_sceneboxes_idx_vs_logical_id**）。
2. uniforms：`uCloudObjIdBase = 72`（= objectCount(0) + boxIdx + 1）、`uCloudFaceArea[4]`、`uEmissiveClamp = 50`（S4 校準延後）、`uR3EmissionGate` R3-3 翻 `0 → 1`。
3. shader CLOUD_LIGHT emissive 分支：`rodIdx = hitObjectID - uCloudObjIdBase` clamp 0~3；`isEmissiveFace = (hitNormal.y > -0.5) && !isInnerFace`（per-rod inner-face 排除見 fix03）；accum 用 `bounceIsSpecular==TRUE || sampleLight==TRUE` LIGHT pattern（sampleLight==TRUE 實被 L779 NEE-miss 回退吃掉，此處等同 primary/specular 才累加；BSDF-indirect 不累加避免與 quadLight[0] NEE 雙計，直到 R3-5b Cloud NEE 補漏 + R3-6 MIS 整合到位）。
4. computeLightEmissions() 完整實作：依 `cloudColorMode` 取 `CLOUD_MODE_K` 查表 → `kelvinToRGB × computeCloudRadiance` 寫入 uCloudEmission[0..3]。`cloudColorCtrl.onChange` 觸發重算 + wakeRender。
5. docs/tests/r3-3-cloud-radiance.test.js（5 組斷言、8 PASS）：LUT 鎖定 / radiance ground truth / K(T) 反比例 / 5600K 單調 / objIdBase 契約。
6. Home_Studio.html cache-buster 迭代 `r3-3-cloud-real-light → r3-3-fix01-boxidx → r3-3-fix01b-gui-guard → r3-3-fix01c-emission-sample-light → r3-3-fix02-sink-dce-guard → r3-3-fix03-inner-face-skip`。

**五輪 debug 教訓**（四條，交接後續 R3-4/5 務必內化）：

- **fix01 `CLOUD_BOX_IDX_BASE` 55→71**：原 plan 把註解「55 東燈條」當成 sceneBoxes.length，忽略複合家具每子塊 push 一次。assertion 才擋下。見 memory feedback_sceneboxes_idx_vs_logical_id。
- **fix01b 移除 `if (mouseControl) setupGUI()` 守門**：Brave 桌面 `'ontouchstart' in window` 常態為 true（指紋保護），導致 Light/Scene/Panels 三組 GUI 全消失。desktop/mobile 皆須無條件建 scene GUI。見 memory feedback_brave_desktop_ontouchstart_guard。
- **fix01c 走錯路（最終被 fix02 取代）**：我誤假設 NEE ray 誤擊 Cloud 造成爆光，改 emissive 條件為 `sampleLight == FALSE`。實際 sampleLight==TRUE 路徑早已被 L779 NEE-miss 吃掉不會走到此分支；真因在 shader 末端 `accumCol += r3Sink`，gate=1 時 sink 變 `Σ(uCloudEmission[i])·gate ≈ (40,31,22)` 直接疊每 pixel。見 memory feedback_pathtracing_dce_sink_gate_trap + feedback_systematic_debugging_check_all_accumcol。
- **fix02 改 sink DCE-guard**：`vec3 r3Sink = uR3EmissionGate * (...)` → `if (uR3EmissionGate < -0.5) { accumCol += ... }`（runtime-impossible guard，compiler 無法 const-fold 否定 uniform reference）。emissive 分支同時回退 LIGHT pattern。
- **fix03 per-rod 內側面排除**：原只排除 -Y 底面，但四支 rod 的內側法向依位置而異（rod 0=-X / rod 1=+X / rod 2=-Z / rod 3=+Z）。加 per-rodIdx `isInnerFace` 判斷，實體燈條扇形弧面朝外、90° 夾角與 GIK 板相鄰的物理正確。角落 butt-join 維持方盒近似（見下方）。

**R3-3 MVP 限制（R3-5b Cloud NEE 補漏 + R3-6 MIS 到位前勿誤認為 bug）**：
- Cloud 目前只在 primary/specular 路徑貢獻（BSDF-indirect 不累加避免與 quadLight[0] NEE 雙計）；**實際視覺：僅見燈管自發光，未見天花板光斑與全室漫射**（2026-04-19 使用者觀察確認）。此為 R3-3 scope 砍半之後果，R3-5b 將 Cloud area sampling 併入 stochastic NEE pool 後修正。
- PRD S3b 規定 curtain-center ROI luminance 比值 [2.8, 3.5] **未量測驗收**（需 R3-5b + R3-6 到位才有完整物理漫射路徑再校）。
- S4 `uEmissiveClamp = median×30` 校準亦延後，目前沿用預設 50；未見明顯 firefly。
- **扇型 rod 幾何**：使用者明示不做，用方盒近似；四角 butt-join（rod 0 x=0.884~0.900、rod 2 x 止於 0.884 切齊）形成硬 90° 折角視覺完整。若將來廠商要做扇型，角落三種處理（穿模 X 瑕疵 / 凹口缺牙 / 45° mitered）擇一；見 R3 遺緒。

**cache-buster**：`r3-3-fix03-inner-face-skip`

---

## R3-3 Cloud 漫射燈條真光源（原計畫內文，作歷史存檔）⬜→✅

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`

**前置條件**：R3-1、R3-2 完成 ✅，R2-17 四根燈條幾何已存在（type = 14 CLOUD_LIGHT，fixtureGroup = 4）。

**產品真值引用**：
- 軟條燈 480 lm/m（D-35NA12V4DR1）
- 東西兩根 2.4m × 480 = 1152 lm／根
- 南北兩根 1.768m × 480 ≈ 848 lm／根
- 全室 Cloud 間接光總計 ≈ 4000 lm

**3-Face Sampling 設計**：

每根燈條是 1.6cm × 1.6cm × L 的細長矩形。6 面中：

| 面 | 法向 | 是否取樣 | 原因 |
|------|------|----------|------|
| y = min（貼 GIK 頂面） | -Y | ❌ 跳過 | 貼死 Cloud GIK 頂面，射出即碰 GIK 背面 |
| y = max（頂面朝天花板） | +Y | ✅ 取樣 | 朝天花板洗亮，形成最主要的間接漫射 |
| x/z 房內側側面 | 向房中心 | ✅ 取樣 | 朝房間中心打漫射 |
| x/z 外側側面（貼牆） | 向外 | ✅ 取樣 | 朝外牆打，洗牆形成牆面氛圍；實測外側面剛好貼齊 GIK 外緣，不被牆直接擋（距牆內面 ≥ 10cm） |
| 兩短端（燈條末端） | ±L 方向 | ❌ 跳過 | 面積太小、無照明意義 |

**採樣流程**（shader 內加 `sampleCloudLight()` 函式）：

```
1. 從 4 根燈條中均勻隨機選一根（均勻 PDF = 1/4，Many-Light 階段改為 MIS）
2. 從 3 面中均勻隨機選一面（PDF = 1/3）
3. 面上均勻取一點（PDF = 1 / face_area）
4. emission direction = face_normal（Lambertian 半球 cosine weighted）
5. 總 selectPdf = 1/4 * 1/3 * 1/face_area
6. BRDF / distance² / cos_emit cos_hit 走標準 NEE 權重
```

**shader 分支新增**：

```glsl
if (hitType == CLOUD_LIGHT) {
    // emission 直接加入 path
    int lightIdx = int(hitObjectID) - CLOUD_LIGHT_BASE_ID;
    accumCol += mask * uCloudEmission[lightIdx];
    break;  // 光源終結 path
}
```

**completion 條件**：
1. ⬜ 4 根燈條在所有 Config 3 畫面下可見發光（emission 作用中，不是 R2-17 那種 emission=0 幾何）
2. ⬜ 關閉某盞燈條（GUI 對應 toggle）後對應根不再發光，其他三根正常
3. ⬜ Cam 1/2/3 × 1000 spp 比對間接光分佈（頂面洗天花板 + 外側洗牆可見）
4. ⬜ 計算 cloudFace 面積代碼於 JS shaderFromApplyPanelConfig 時動態算好，寫進 uniform `uCloudFaceArea[4]`（4 根 × 3 面 = 12 個面；或拆 3 個 vec4 uniform）
5. ⬜ 無 fireflies（高能量 outlier spike）；若出現需加 clamp 上限（uEmissiveClamp）
6. ⬜ 產品 lm 標示 slider（480 lm/m）調整後畫面亮度依比例變化

**cache-buster 建議**：`r3-3-cloud-real-light`

---

## R3-4 軌道投射燈真光源（spot cone NEE）✅（2026-04-19 完工）

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`

**前置條件**：R3-3 完成 ✅，R2-14 四盞軌道投射燈頭幾何已存在（type = 13 TRACK，fixtureGroup = 1）。

**產品真值引用**：
- 22W 變焦 COB 調光型軌道燈（Varilumi）
- 光通量 2000 lm（最大）
- 光束角 15° ~ 60° 連續可調
- 色溫 3000 / 4000 / 6000K 三擇一（建議 4000K）
- 燈頭半徑 r = 3cm，emitter 面積 ≈ 2.827e-3 m²
- 4 盞總光通量最大 8000 lm

**spot cone 採樣設計**：

```
emitter 位置：uTrackLampPos[i]（舊 uniform，R2-14 已存在）
emitter 方向：uTrackLampDir[i]（舊 uniform）
beam 內外半角：uTrackBeamCos[i] = vec2(cos(inner), cos(outer))
  inner 預設 15°（邊緣平滑起點）
  outer 預設 30°（= 全角 60°，落在產品 upper range）

NEE 採樣方向：
  1. 在 beam outer cone 內均勻採樣方向 d_samp（Frisvad orthonormal basis）
     PDF_dir = 1 / (2π * (1 - cos(outer_half)))
  2. 從 emitter 面上均勻取點 p_samp
     PDF_pos = 1 / emitterArea
  3. 射線從 hit 點朝 p_samp
  4. smoothstep(outer_cos, inner_cos, dot(-d_light, lampDir)) 做邊緣衰減
  5. 乘上 emission 與 BRDF，除 distance²
```

**shader 分支新增**（TRACK 命中時加 emission）：

```glsl
if (hitType == TRACK) {
    int lampIdx = int(hitObjectID) - TRACK_BASE_ID;
    vec3 d_to_hit = normalize(hitPoint - uTrackLampPos[lampIdx]);
    float cos_ax = dot(d_to_hit, uTrackLampDir[lampIdx]);
    float attenuation = smoothstep(uTrackBeamCos[lampIdx].y, uTrackBeamCos[lampIdx].x, cos_ax);
    accumCol += mask * uTrackEmission[lampIdx] * attenuation;
    break;
}
```

**完成條件**：
1. ✅ 4 盞投射燈打牆面 GIK / 地板 / 家具形成可見光斑（非 R2-14 emission=0 黑燈頭）
2. ✅ 光斑邊緣 smoothstep 衰減自然（使用者肉眼確認「直接光周圍柔邊漂亮」）
3. ⬜ 光束角從 15° 調到 60° 畫面光斑擴張（本階段 beam 固定 60°，可調性延後至 R3-5a 或 R4）
4. ✅ 色溫 3000K / 4000K / 6000K 可即時切換，光斑顏色正確偏暖 / 中性 / 偏冷（使用者確認「色溫切換功能正常」）
5. ✅ lumens 0 → 2000 連續滑動亮度線性變化（使用者確認「隨拉桿變大變小，任何數值皆符合視覺預期」）
6. ✅ Cam 1/2/3 無 firefly（uEmissiveClamp=50 max-channel normalize）
7. ✅ 光源本體自發光幾何（emitter 面）不過曝（fix03 per-face gate + fix07 radiometric 量綱修正）

**最終落地實作**（與原計畫差異記錄）：
- 採 Option A'：emissive primary/specular + 5 選 1 stochastic NEE（吸頂 quadLight[0] + 4 盞 track，selectPdf=1/5）；未走 beam cone uniform 採樣（改以 emitter disk 點採樣 + smoothstep falloff 近似，更符合 NEE helper 集中化）
- fix01~05：TRACK_LIGHT=15 hitType 分家、per-face gate、max-channel normalize、NEE target 修正、firefly clamp
- **fix07（本輪最終根因修復）**：`computeTrackRadiance` 由 `Φ/(Ω·A)`（photometric cd/m²，overshoot ~1200×）改為 `Φ/(K·π·A)`（radiometric W/(sr·m²)，對齊 Cloud）；shader NEE helper 補 `× TRACK_LAMP_EMITTER_AREA` disk-area integrand；移除 fix05 throughput-50 clamp 與 fix03 bounces==0 tier-10 clamp（統一 uEmissiveClamp=50）

**cache-buster 最終版**：`r3-4-fix07-radiometric-unit`

---

## R3-5a 軌道廣角燈真光源 ✅（2026-04-19）

**最終落地**（使用者肉眼 4 條件全通過：光斑柔邊、lumens 線性、色溫切換、無 firefly + 無回歸）：
- shader：新 hitType `TRACK_WIDE_LIGHT = 16`、新 uniforms `uTrackWideBeamCos[2]` / `uTrackWideLampIdBase = 700` / `TRACK_WIDE_LAMP_EMITTER_AREA = π·0.05²`；`sampleStochasticLight5` → `sampleStochasticLight7`（selectPdf = 1/7）；BoxIntersect wide loop 加 face-gate（底蓋→emitter、側+頂→DIFF housing）；新 `TRACK_WIDE_LIGHT` hit 分支鏡像 R3-4 pattern；10 處 NEE 呼叫點更名；DCE sink 擴充 uTrackWideBeamCos / uTrackWideLampIdBase
- JS：`TRACK_WIDE_*` 8 常數 + `trackWideLumens = 2500`；`computeTrackWideRadiance(lm, T_K, A) = Φ/(K·π·A)` 量綱對齊 R3-4 fix07；uniform 初始化 + 雙層 assertion；南/北色溫 dropdown onChange 補 `computeLightEmissions()`；新 GUI slider「南北廣角燈 lm」0~2500
- cache-buster：`r3-5a-track-wide-nee`
- 測試：`docs/tests/r3-5a-track-wide-radiance.test.js` 12 斷言全 PASS；R3-4/R3-3/R3-2 regression 不破
- 已知遺留：舊專案甜蜜點 UI（打燈角度 / 光束角度 / 軌道離 GIK 間距 / 同側燈間距）未復刻，使用者裁定延後至 R4 UI 控制層



**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`

**前置條件**：R3-4 完成 ✅，R2-15 兩盞廣角燈頭幾何已存在（type = 13 TRACK 但 fixtureGroup = 2，uWideTrackLightEnabled）。

**產品真值引用**：
- 25W 廣角散光調光型軌道筒燈（Varilumi）
- 光通量 2500 lm（最大）
- 光束角 120°（固定，半角 60°）
- 色溫 3000 / 4000 / 6000K 三擇一
- emitter 半徑 r = 5cm，面積 ≈ 7.854e-3 m²
- 2 盞總光通量最大 5000 lm

**做法（完全複用 R3-4 pattern）**

sampleStochasticLight5 → sampleStochasticLight7：
- slot 0：quadLight[0] 吸頂燈（現況）
- slot 1-4：Track spot 4 盞（R3-4 已落地）
- slot 5-6：TrackWide 2 盞（本階段新增）

新 uniform：
- `uTrackWideBeamCos[2]` = vec2(cos(55°), cos(60°))（固定值，南北獨立仍可設不同）
- `uTrackWideEmission[2]`（R3-1 已預留，本階段填入實值）
- `TRACK_WIDE_LAMP_EMITTER_AREA = PI · 0.05²`（disk-area integrand 用，類 R3-4 fix07 常數）

JS 新增 `computeTrackWideRadiance(lm, T_K, A) = Φ/(K·π·A)`（與 Cloud/Track 量綱完全一致）。selectPdf 改為 1/7。

**為何此階段風險低**：
- R3-4 fix07 已釐清 radiometric Φ/(K·π·A) 量綱契約，本階段直接複用
- emitter 是 disk，非 area emitter，與 Track spot 同類
- BeamCos 55°/60° 軟邊緣較 spot 15°/30° 更平滑，不易出 firefly
- 不涉 MIS / area sampling 任何新演算法

**完成條件**：
1. ⬜ 2 盞廣角燈（南/北）打房間中央與天花板形成可見柔邊光斑
2. ⬜ lumens 滑桿 0~2500 連續拉動亮度線性變化
3. ⬜ 色溫 3000/4000/6000K 三檔切換光斑色偏正確
4. ⬜ Cam 1/2/3 × 500 spp 無 firefly，既有 Track/Cloud 視覺不回歸
5. ⬜ docs/tests/r3-5a-track-wide-radiance.test.js 通過（測項仿 r3-4-track-radiance.test.js 11 項）

**cache-buster 建議**：`r3-5a-track-wide-nee`

**OMC 工具**：Claude Code 直接動手（pattern 複用，ralplan overkill）。

---

## R3-5b Cloud 漫射燈條 NEE 補漏 ✅

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`

**前置條件**：R3-5a 完成 ✅（sampleStochasticLight7 基礎建設就緒）。

**背景**

R3-3 完工時僅落地 Cloud emissive 幾何與 radiance 量綱修正，原計畫（見下方「原 R3-3 計畫歷史存檔」章節）之 3-face area sampling NEE 未實作。現況：
- shader L1321-1322：`if (bounceIsSpecular == TRUE || sampleLight == TRUE) accumCol = min(mask * emission, clamp)`
- sampleLight==TRUE 路徑被 L779 NEE-miss 吃掉，實際等同 primary/specular 才累加
- BSDF-indirect 累加被阻斷
- 結果：Cloud 對除 primary/specular 以外任何表面貢獻 = 0（使用者 2026-04-19 觀察確認）

本階段目標：將 Cloud 4 根 rod 納入 stochastic NEE pool，實作 area sampling。

**做法**

sampleStochasticLight7 → sampleStochasticLight11：
- slot 0-6：R3-5a 已就緒（1 quad + 4 Track + 2 Wide）
- slot 7-10：Cloud rod 4 根 area sampling（本階段新增）

每根 rod 採樣流程：

```
1. per-rod 3-face uniform 選面（排除 -Y 貼 GIK 底面與 per-rod 內側面）
   面權重可依 face-area 加權（rod 0/1：側面 +X/-X vs 頂面 +Y 面積不同）
   或簡化 uniform 1/3（前提是三面面積近似）
2. 選中面上 uniform 取 barycentric 隨機點
3. 從 shading point x 連線到 sample 點，計算 dir、distance²
4. emission direction = face_normal；cos_emit = max(0, dot(-dir, face_normal))
5. throughput = emission × cos_emit × faceArea / distance² / selectPdf
   （face-area integrand，取代 R3-4 fix07 disk-area integrand 之類比）
```

shader L1321 gate 放開：

```glsl
// 從 `if (bounceIsSpecular == TRUE || sampleLight == TRUE)`
// 改為允許 sampleLight==TRUE 真正累加（不再被 L779 NEE-miss 先吃）
// 須同步確認 L779 NEE-miss handler 對 CLOUD_LIGHT target 不誤爆
```

**關鍵設計敏感點**（ralplan Critic 必審）：
1. face 選擇 PDF：uniform 1/3 vs 面積加權 1/A 之正確性
2. inner-face 排除：per-rodIdx 0=-X / 1=+X / 2=-Z / 3=+Z（同 R3-3 fix03）
3. selectPdf = 1/11 × (1/3 面選擇) × (1/A 點選擇) 之 Monte Carlo integrand 完整性
4. 雙計風險重驗：quadLight[0] 是吸頂燈 quad（Z 偏置、朝下 Lambertian），與 Cloud rod 側面/頂面發光不重疊；R3-3 時期「避免與 quadLight[0] 雙計」之論在新架構下是否仍成立
5. R3-6 MIS 整合前，BSDF-indirect 是否也要同步放開（或等 MIS 權重規則到位才動）

**完成條件**：
1. ✅ Cloud 4 rod 打天花板形成 口 字形柔光帶（使用者 2026-04-20 肉眼確認）
2. ✅ 房間四牆上半部可見 Cloud 漫射反射（user：「這版的燈光終於正確了」）
3. ✅ lumens 滑桿 0~480 lm/m 連續拉動房間整體亮度線性變化
4. ✅ Cam 1/2/3 × 876 spp 無 firefly
5. ✅ 關 track / wide checkbox 時牆上無殘留冷暖光斑（fix07 NEE pool gate 對稱）
6. ⬜ docs/tests/r3-5b-cloud-nee.test.js contract test 延後至 R3-6 MIS 整合同批補（ADR 登錄）

**實作摘要（與 ralplan 甲案 ADR 偏離處）**

ralplan DELIBERATE 共識原案為 2-face 甲案（+Y 頂 ∪ 外長側，face-pick 1/2 補償）。執行階段發現兩個肉眼無法通過的問題：

1. **2-face 甲案的 cos(θ) 硬邊**：軸對齊 normal 造成天花板出現「東西南北四軸投射」形貌，不符 user 唯一肉眼驗收基準「口字柔光帶」。改 per-rod 單一 45° 對角 Lambertian normal（E/W rod: `(±1,1,0)/√2`；S/N rod: `(0,1,±1)/√2`），以分散發射方向破離散 cos(θ) 硬邊。
2. **單點 target → 4 離散光斑**：原案 target=rod 中心 + normal·halfExtent 單點，4 rod → 天花板 4 離散亮點。改 `longAxisJitter = rng()*2-1` 沿長軸 uniform 分佈，2.4m 線段 emitter 還原。

NEE pool gate 對稱為執行階段新發現項（非 ralplan 原案覆蓋範圍）：關 track / wide checkbox 時側牆仍殘留冷暖光斑，追到 sampleStochasticLight11 Track idx 1-4 與 Wide idx 5-6 分支無 `uXxxLightEnabled` gate，僅 Cloud idx 7-10 有。補 mirror gate 後三類光源 NEE 鏡像對稱，GUI checkbox 才真正可關。

詳見 `Debug_Log.md` R3-5b 段（fix04~fix07 四連翻車解析）。

**cache-buster**：`r3-5b-fix07-nee-gate-track-wide`

**OMC 工具**：`/oh-my-claudecode:ralplan`（甲案共識）→ `/oh-my-claudecode:ralph`（fix04~fix07 四次迭代）實際採用。

---

## R3-6 Many-Light Sampling + MIS 整合 ✅

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`

**前置條件**：R3-5b 完成 ✅（11 盞 stochastic NEE pool 全到齊）。

**背景**

R3-5b 末 sampleStochasticLight11 已涵蓋全部 11 盞（1 吸頂 quad + 4 Track + 2 Wide + 4 Cloud），但仍為「stochastic 1-of-11 uniform pick + selectPdf=1/11」架構，缺：
- 光源重要性加權（power-proportional sampling）
- MIS weight（NEE 與 BSDF sample 兩路）

**Part A：Many-Light importance sampling（可選擴充）**

目前 uniform 1/11 對高流明 Track（2000 lm）與低流明 Cloud single face（~400 lm/face）一視同仁。升級為 power-proportional：

```
selectPdf[i] = flux_i / Σflux
```

高流明光源被選中機率提高，variance 降低。

**Part B：MIS Power Heuristic（β = 2）**

每個 diffuse bounce 做兩條 shadow ray：

```
1. NEE sample：對選中的光源做採樣，得 pdf_nee
2. BRDF sample：對 BRDF cosine-weighted 取樣，若命中光源則得 pdf_brdf
3. MIS 權重（power heuristic）：
   w_nee  = pdf_nee²  / (pdf_nee² + pdf_brdf²)
   w_brdf = pdf_brdf² / (pdf_nee² + pdf_brdf²)
4. 最終 contribution = w_nee * L_nee + w_brdf * L_brdf
```

對光滑表面（低 roughness）BRDF sample 收斂快；對粗糙表面 NEE 收斂快。MIS 自動切換。

**Part C：BSDF-indirect 累加回開**

R3-3 / R3-5b 為避雙計，BSDF-indirect 到 Cloud 可能仍被阻斷。MIS 權重到位後，雙計問題由 w_nee / w_brdf 機制解決，可安全回開：

```glsl
if (bounceIsSpecular == TRUE || sampleLight == TRUE) { ... }
// 改為 BSDF-indirect 也累加，但 mask 已乘 w_brdf
```

**完成條件**：
1. ✅ Many-Light 實測：fps 較 R3-5b 無感差異（使用者 2026-04-20 肉眼回報「MIS 初期螢火蟲較少、收斂較快」）
2. ✅ MIS 上線後，無螢火蟲雜點、無光錐外漏光（2026-04-20 使用者肉眼驗收 PASS）
3. ✅ Cam 1/2/3 無 firefly（2026-04-20 肉眼驗收 PASS）
4. ✅ Config 3 下開啟全部 11 盞燈，畫面亮度分佈均勻（fix04 補 Cloud checkbox 後 UI 層完整）
5. ✅ R3-3 時期擱置之「Cloud 雙計風險論」正式解除記錄（MIS w_nee + w_brdf 機制到位）

**後續補丁（2026-04-20）**
- **fix04**：Cloud 漫射燈條 GUI checkbox 補齊（shader 三處 gate 早在 R3-5b 就位，R3-6 落地時僅漏 UI 層 checkbox，fix04 補齊與 Track/Wide 對稱）
- **fix05**：天花板 1e Center bmax.z 3.056 → MAX_Z 延至南牆外邊界（R2 fix23 遺留的中央 x=[-1.91,1.91] z=[3.056,MAX_Z] 缺口，南側仰視露黑楔）
- **fix06**：地板 0e 對稱補長 + 西南/東南角柱（box 14/15）cullable 2→3 新增 X-only tier（南牆 X-ray 剝離時柱子保持可視，只跟東西側牆連動）

**cache-buster**：`r3-6-fix03-mis-math` → `r3-6-fix04-cloud-checkbox` → `r3-6-fix05-ceiling-s-extend` → `r3-6-fix06-pillar-xonly-floor-sym`

**OMC 工具**：`/oh-my-claudecode:ralplan`（deliberate 甲案 APPROVE iter 2）→ `/oh-my-claudecode:ultrawork`（MIS 落地）→ 收尾 fix04~06 單檔直改。

---

## R3-7 indirectMul 校準歸一 ⬜

**目標檔案**：
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js`
- `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl`

**前置條件**：R3-6 完成 ✅。

**背景**：
R2-18 `uIndirectMultiplier = 1.7` 是 Phase 2 框架 2-bounce truncation 下的肉眼補償。R3-6 MIS 上線後，理論上提高 max_bounces 能自然收斂到正確能量，不必靠 1.7。

**做法**：

```
Step 1：max_bounces 從 4 提到 6
  → Cam 1/2/3 × 2000 spp 截圖（indirectMul=1.7 / 1.5 / 1.3 / 1.0 各一輪共 12 張）
  → 比對各級平均亮度分佈

Step 2：max_bounces 從 6 提到 8
  → 同樣四級 indirectMul，12 張截圖

Step 3：決定最終組合
  → 若 max_bounces=8 + indirectMul=1.0 與 R2-18 (bounces=4, mul=1.7) 亮度分佈接近（± 5%），則歸一成功
  → 若仍偏暗，保留 indirectMul 並記錄最終值
```

**R3-0 魔數同步處理**：
若 indirectMul 可歸 1.0，順便檢查 uLegacyGain 是否也可歸 1.0（兩者可能有部分耦合）。

**完成條件**：
1. ⬜ max_bounces 從 4 提到 8，fps 降幅不超過 30%
2. ⬜ 與 R2-18 定案版（900 + 1.7）對比亮度差異量化記錄於 Debug_Log
3. ⬜ 若 indirectMul 歸 1.0，GUI 將此 slider 標示「R3 歸一」；否則保留並註記
4. ⬜ 若 uLegacyGain 歸 1.0，移除 10 處 uniform 呼叫並簡化 shader

**cache-buster 建議**：`r3-7-indirect-normalize`

---

## R3-8 採購評估驗收（4 情境切換測試）⬜

**目標檔案**：無程式碼改動，純驗收。

**前置條件**：R3-7 完成 ✅。

**4 個情境**：

```
情境 1：投射燈 22W/2000 lm → 假設升級 30W/3000 lm（lumens slider 調 3000）
  驗證：牆面 GIK 光斑亮度提升 1.5 倍，間接光跟著變亮

情境 2：Cloud 軟條燈 4W/m/480 lm/m → 假設升級 8W/m/960 lm/m（lumens slider 調 960）
  驗證：天花板與側牆間接光提升 1 倍，房間整體 ambient 明顯變亮

情境 3：色溫 4000K → 3000K 全室切換
  驗證：整體氛圍從中性偏暖色，牆面 GIK（紅色 / 白色兩組對比）色偏合理

情境 4：減少一盞投射燈（僅留 3 盞，東北角或西南角關閉）
  驗證：關閉側有可辨識的暗部（但非全黑），其他 3 盞 fill-in 後仍可工作
```

**每情境驗收流程**：
- Cam 1 / Cam 2 / Cam 3 × 1000 spp 截圖（前後對比共 8 張）
- 平均亮度、死角位置人工判讀
- 無 firefly、無 variance bias

**完成條件**：
1. ⬜ 4 情境切換後畫面符合物理直覺（亮度、色偏、陰影分佈）
2. ⬜ 使用者確認「這個工具真的可以拿來評估採購決策」
3. ⬜ 至此 R3 全部 ✅，Home_Studio_3D 專案達到「光通量單位正確 × 架構經 MIS 驗證 × 採購評估功能可用」三重標準

**cache-buster 建議**：`r3-8-final-verification`

---

## 驗證紀律（每階段通用）

- 每個 R3-N 完工前 Cam 1 / Cam 2 / Cam 3 × 至少 500 spp 截圖比對
- 禁止在單 Cam 低 spp 下宣告成功（R2-14 fix04 反例）
- 截圖存至 Home_Studio_3D/snapshot 資料夾，命名 `YYMMDD-camN-R3-N-spp.png`

## 快取／驗證網址紀律

- URL：`http://localhost:9001/Home_Studio.html?v=<cache-buster>`
- 嚴禁加 `Home_Studio_3D/` 前綴（server cwd 已在資料夾內，會 404）
- 嚴禁 `?_t=N` 純流水號
- query 變化 → 不必提 Cmd+Shift+R
- query 不變才提醒硬刷新，且 URL 與提示分行

## Debug 紀律

依 SOP/Debug_Log.md 通用紀律：
1. 複用 material type 前必先 Read 該 type shader 完整分支
2. material type 命名必須為物件語義（TRACK、CLOUD_LIGHT）而非材質特徵
3. debug artifact 時第一步 Read 該 hitType 分支，未讀前禁止提幾何 / shadow / BVH 假說

---

## R4 展望（不屬於 R3）

R3 完成後可能的 R4 方向（需使用者另行開啟）：
- 日光入射（窗外太陽光實測照度 + 時辰／方位）
- 隔音窗透射率物理模型（玻璃折射 + 室外景背板雙向照明）
- 頻譜 rendering（CRI 可視化、LED 光譜非完美白）
- Volumetric scattering（煙霧、空氣中微粒散射，模擬「光柱」效果）

以上皆非 R3 範圍，避免 scope creep。

---

## 檔案對照索引

| 項目 | 路徑 |
|------|------|
| 本 SOP | `/Users/eajrockmacmini/Documents/VS Code/My Project/SOP/R3：燈光系統.md` |
| 產品規格真值 | `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/docs/R3_燈具產品規格.md` |
| 通用 Debug 紀律 | `/Users/eajrockmacmini/Documents/VS Code/My Project/SOP/Debug_Log.md` |
| R2 完工 SOP | `/Users/eajrockmacmini/Documents/VS Code/My Project/SOP/R2：所有幾何物件.md` |
| 主專案 JS | `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/js/Home_Studio.js` |
| 主專案 shader | `/Users/eajrockmacmini/Documents/VS Code/My Project/Home_Studio_3D/shaders/Home_Studio_Fragment.glsl` |
| 舊專案（僅搬數字） | `/Users/eajrockmacmini/Documents/VS Code/My Project/Home Studio 3D Pace Tracing/Path Tracking 260412a 5.4 Clarity.html` |
