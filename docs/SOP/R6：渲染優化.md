# R6：渲染優化 — 總體進度地圖

> 一張地圖看「現在在哪、前面做過什麼、後面要做什麼」
> 細節全部指向專門檔案、本份只當 R6 階段總覽

---

## 📍 你現在在這裡

```
階段       R6-3 視覺收斂優化結案整理
狀態       到此為止；WebGL path tracing 低 SPP 修補線停止加碼
痛點       C3 / C4 移動時 1 / 2 / 4 SPP 仍有滿版黑白點，形成視覺障礙
最新證據   v22d：C3 / C4 移動降到 2SPP 並跳過 borrow pass 後，使用者肉眼仍判定黑點太多
目前判讀   目前架構內的局部修補已到低 ROI；真正需要的是移動 preview 與靜止 final 分流
下一問題   下一窗 AI 先接 R7 丁 blue noise 小實驗；WebGPU 僅保留小型 hybrid preview 概念驗證
目標       R6 文檔收束；保留可用成果與 no-go 紀錄，避免後續重試同類方案
```

---

## 進度地圖

```
[已完工]
  R4 全階段                     ✅ DONE
  R5 Cloud 光源重構              ❌ 撤回（物理上限、2026-04-26）

R6 渲染優化（三主線並行）
┌─ 主線 1  後製調色             ✅ 完工 2026-05-01
│   階段 1      雙邊濾波          ❌ 撤回
│   階段 1.5    LGG / ACES / WB / Hue    ✅ 完工
│
├─ 主線 2  BVH 加速             ⏸ 留候補
│   Phase 1.0    盤點             ✅
│   Phase 1.5    SAH / leaf       ❌❌
│   桶 4 F1      frame-skip       ✅ 結案
│   桶 4 F2      timer            ❌
│   桶 2 #2      Russian Roulette ⏳ 待命（廠商嫌慢才動）
│
└─ 主線 3  視覺收斂優化          🧾 結案整理   ← 📍 你在這
    Phase 0   收斂盤點             ✅
    Phase 1A  Cloud rod selection / slot stratification    ❌ no-go
    Phase 1B  Cloud 2D target sampling                     ❌ no-go
    Phase 1C  Cloud arc formula / PDF / MIS contract        ✅
    Phase 2   Cloud probe isolation / low-SPP movement       ✅ 已收束
      Step 1  visibility / facing probe                     ✅
      Step 2  sampling budget diagnostic                    ✅
      Step 3  MIS weight / BSDF-hit probe                   ✅
      Step 4  direct NEE screen-band probe v10              ✅
      Step 5  top band hotspot / percentile probe              ✅
      Step 6  visible surface / hotspot / dark-fill / movement protection  ✅
      End     WebGL path tracing 低 SPP 修補線停止

    後處理型路線（SVGF / reproject / OIDN）保留候補，
    下一窗 AI 先走 R7 丁 blue noise 小實驗。
    WebGPU / hybrid preview 僅保留小型概念驗證，不進整套搬遷。

R7 採樣演算法升級               ⏳ 未來展望
                                 觸發條件 = R6-3 達不到目標
```

---

## 主線 1：後製調色 ✅ 完工

A / B 雙模 + LGG（Lift / Gamma / Gain）+ ACES 色調映射 + 白平衡 + 色相旋轉 + per-config 預設體系全套落地。
完工日期 2026-05-01、累計三段子線：r6lgg14（ACES + EXP 甜蜜點）/ r6lgg29（J3 暗角借光）/ r6lgg30（WB + Hue + 多 config 切換）。

```
完工現況詳細     →  .omc/HANDOFF-R6-r30-final.md
歷史教訓總集     →  .omc/LESSONS-R6-shadow-lift-failures.md
Debug 紀錄        →  docs/SOP/Debug_Log.md 章節「R6-LGG-J3」「R6-LGG-r30」
```

---

## 主線 2：BVH 加速 ⏸ 留候補

四連敗確認 GPU speed 已飽和（SAH builder -39% 回滾 / leaf packing ≤ 1% / 桶 4 F2 probe overhead ≥ 1.24% / 桶 2 #2 RR ralplan 通過但執行階段未啟動）。
廠商現場若 1024 spp 30 ~ 34 秒嫌慢、再啟動桶 2 #2 Russian Roulette。

```
入口 + ROADMAP    →  .omc/HANDOVER-R6-2.md
RR 啟動指引       →  .omc/HANDOFF-next-russian-roulette.md
```

---

## 主線 3：視覺收斂優化 🧾 ← 你在這

> 動機：C3 / C4 切換後初期雜點過大，使用者因此被迫用固定機位。
> 目標：C3 / C4 從 0 spp 開始 30 ~ 60 秒內收到肉眼可接受。
> 結案範圍：R6-3 到此為止；下一輪先做 R7 丁 blue noise 小實驗，再評估 R7 丙與 WebGPU 小實驗。

### 目前總結

```
已完成
  Phase 0 量測：
    C3 / C4 與 C1 / C2 的 wall-clock 差距小，主要痛點是 variance / cleanup tail。
    C3 = Cloud diffuse only。
    C4 = track + wide lights only。

  Phase 1A / 1B：
    Cloud rod selection / early-spp slot stratification：使用者肉眼 no-go。
    Cloud 2D target sampling：使用者肉眼 no-go。
    後續不重包同類 target-shape tweak。

  Phase 1C：
    Cloud 1/4 arc、radiance、area、PDF、MIS contract 已落地。
    1600 lm/m 是使用者決策後的 C3 Cloud-only 設計值。

  Phase 2：
    Cloud sampling budget 正常，C3 activeLightIndex = [7, 8, 9, 10]。
    direct NEE MIS 權重健康，directNeeAverageWeight = 1。
    v7 前 BSDF-hit 讀值有污染，舊 bsdfHitAverageWeight 不再作判斷依據。
    forced BSDF-hit probe v8b 可工作，但自然 BSDF-hit 到 64 samples 仍是 0 event。
    direct NEE screen-band v10 指向 top band 高亮貢獻集中。
    direct NEE top-band percentile v11 確認 top band 由少數超亮樣本主導。

  Movement protection / first-frame：
    v19 解掉 1SPP 黑幕，但快照開啟會卡，已做快照開關且預設關閉。
    v20 ~ v22 多輪顯示端保護已驗證。
    舊畫面混合會有殘影。
    顯示端模糊會糊或卡。
    簡化 preview 會閃出灰色廉價模型。
    硬算 4SPP 會拖低 FPS。
    v22d 改 C3 / C4 移動 2SPP 並跳過 borrow pass，使用者仍判定黑點太多。

結案判讀
  目前 WebGL path tracing 的低 SPP 畫面直接拿來當移動畫面，會形成視覺障礙。
  1 / 2 / 4 SPP 都不足以提供乾淨互動視線。
  R6-3 的有效成果是定位根因、排除低 ROI 路線、保留診斷工具與可用小修。
  後續 ROI 最高方向先從可逆小實驗開始，再評估移動 preview 與靜止 final 分流。

下一窗交棒
  第一順位：R7 丁 blue noise 小實驗。
  第二順位：R7 丙光源機率優化。
  第三順位：WebGPU / hybrid preview 小型概念驗證。
  入口：.omc/HANDOFF-R6-3-closeout-to-R7-small-experiments.md
```

### 最新 v11 判讀

```
機器驗證：
  await reportCloudDirectNeeTopBandPercentileProbeAfterSamples(64, 120000)

條件：
  URL = http://127.0.0.1:9004/Home_Studio.html
  config = C3
  camera = cam1
  targetSamples = 64
  uniformTopBandSentinelPass = true

top band：
  p50 = 0.1886456
  p90 = 4.087589
  p99 = 38.88792
  max = 165.7287
  averageContributionLuma = 2.000147

比例：
  p90 / p50 = 21.668086
  p99 / p50 = 206.142735
  max / p50 = 878.518767

白話判讀：
  top band 的典型事件偏低。
  但最亮那一小撮事件非常亮。
  這支持「少數超亮 direct NEE 樣本拖慢早期收斂」。
```

### v12 最新量測

```
實作：
  reportCloudDirectNeeDiffuseCountSplitProbeAfterSamples()

目的：
  把 Cloud direct NEE 分成：
    primary-surface Cloud NEE
    bounced-surface Cloud NEE

真頁面條件：
  http://127.0.0.1:9004/Home_Studio.html
  C3
  cam1
  targetSamples = 8
  script token = r6-3-cloud-direct-nee-diffuse-count-split-v12

結果：
  allDirectNeeContribution.contributionMass = 2428004.481752
  primaryDirectNeeContribution.contributionMass = 0
  bouncedDirectNeeContribution.contributionMass = 2428004.481752

使用者端 Console 驗收：
  allContributionMass = 8896107.614104
  primaryContributionMass = 0
  bouncedContributionMass = 8896107.614104
  splitContributionMass = 8896107.614104

比例：
  primaryContributionShare = 0
  bouncedContributionShare = 1
  splitVsAllContributionRatio = 1
  dominantDirectNeeSurfaceClass = bouncedSurface

白話判讀：
  Cloud direct NEE 的亮度貢獻，目前看起來集中在反彈後的表面。
  使用者端驗收與自動化實頁驗證比例一致。
  這把 ROI 推向 indirect diffuse cleanup tail。
```

### v13 / v14 最新量測

```
使用者肉眼線索：
  地板與 GIK 板看起來最髒。

v13 先查：
  floor + GIK priority probe

v13 結果：
  floorContributionShare = 0.000576
  gikContributionShare = 0.004858
  floorPlusGikContributionShare = 0.005434
  otherContributionShare = 0.994566

v14 再拆 otherSurface：
  reportCloudBouncedDirectNeeReceiverClassProbeAfterSamples(4, 120000)

v14 結果：
  ceiling = 0.635714
  wall = 0.343787
  object = 0.015065
  floor = 0.000576
  gikPanel = 0.004858
  classifiedVsBouncedContributionRatio = 1

白話判讀：
  畫面上髒點看起來在地板與 GIK 板附近。
  量測顯示主要 contribution 來源是天花板，其次是牆面。
  地板 + GIK 板合計只佔約 0.54%。

判讀修正：
  v14 的主要價值是確認 receiver-class 分類與讀值路徑有接對。
  ceiling / wall 總 contribution 最大符合直覺，對「地板 / GIK 可見螢火蟲很多」的解釋力有限。
  若 probe 只是在檢查 uniform、cache-bust、readback、分類加總或分類是否錯位，回報時必須明講「這是儀器檢查」。
  儀器檢查不得包裝成已經解決畫面問題。
  接下來真正要回答的是：
    可見表面上的 firefly / hotspot 密度與亮尾端分布。

目前最高 ROI：
  v15 visible-surface firefly / hotspot probe 已完成。
  v16 dark visible-surface source probe 已完成。
  v16 dark visible-surface cleanup candidate 已完成初步數據驗證，但使用者肉眼 no-go。
  v17 bright sample coverage probe 已完成。
  下一步改做守門式同表面補暗候選：
    floor / GIK 在低 SPP 時，大多數可見樣本仍在等正常亮度補齊。
    先使用同片表面已抽到正常亮光的像素，補給附近還偏暗的相似像素。
    補給必須用法線、材質、距離守門，避免跨邊界抹光。
    低 SPP 時較強，SPP 變高後逐步退場。
```

### v15 可見表面異常高亮點 probe

```
新增 helper：
  reportCloudVisibleSurfaceHotspotProbeAfterSamples()

目的：
  直接量畫面第一眼看到的表面。
  指標改成每類表面自己的亮尾端與異常高亮密度。
  這一步回答「哪一類可見表面真的比較容易出現螢火蟲髒點」。

分類：
  floor
  gikPanel
  ceiling
  wall
  object

實頁條件：
  pageUrl = http://127.0.0.1:9004/Home_Studio.html
  scriptSrc = js/Home_Studio.js?v=r6-3-cloud-visible-surface-hotspot-v15
  currentPanelConfig = 3
  currentCameraPreset = cam1
  targetSamples = 8

實頁結果：
  dominantVisibleSurfaceHotspotClass = ceiling
  dominantVisibleSurfaceHotspotDensity = 0.079704
  floorGikHotspotPixelDensity = 0.036074

  per-surface：
    ceiling:
      hotspotPixelDensity = 0.079704
      p50 = 0.1454656
      p99 = 52.66418
      max = 67.18923
      maxToP50Ratio = 461.890853

    wall:
      hotspotPixelDensity = 0.057561
      p50 = 0.1074137
      p99 = 12.07332
      max = 165.7287
      maxToP50Ratio = 1542.900952

    gikPanel:
      hotspotPixelDensity = 0.043426
      p50 = 0.01231235
      p99 = 6.303923
      max = 58.44717
      maxToP50Ratio = 4747.036106

    object:
      hotspotPixelDensity = 0.039702
      p50 = 0.02804233
      p99 = 7.496671
      max = 124.8972
      maxToP50Ratio = 4453.880972

    floor:
      hotspotPixelDensity = 0.023925
      p50 = 0.06116075
      p99 = 26.33209
      max = 82.06091
      maxToP50Ratio = 1341.725044

判讀：
  以每個可見像素的異常高亮密度看，天花板最高，牆面第二。
  GIK 板不是最高密度，但 maxToP50Ratio 最高。
  白話說：GIK 板平常偏暗，一旦出現亮點就會非常刺眼，所以肉眼很容易覺得髒。
  地板密度最低，但 p99 和 max 仍高，代表它也有可見亮點，只是每可見像素密度沒有天花板 / 牆面 / GIK 高。

重要限制：
  targetSamples = 8 的數字與 targetSamples = 4 呈現等倍放大。
  這代表目前 helper 主要讀同一個隔離樣本分布，不能把 8 samples 解讀成 8 次獨立隨機穩定性。
  結論可用來排序「同一張隔離樣本裡哪類表面更容易髒」。
  後續若要談穩定性，需要新增會推進隨機樣本的讀回方式。

下一步：
  優先測試 ceiling / wall 的 bounced Cloud direct NEE cleanup candidate。
  同時保留 GIK 的暗表面刺眼問題，後續可能需要材質或亮點壓制策略一起處理。
```

### v16 暗表面來源拆解與候選清理

```
新增 helper：
  reportCloudDarkVisibleSurfaceHotspotSourceProbeAfterSamples()
  reportCloudDarkSurfaceCleanupCandidateAfterSamples()
  setCloudDarkSurfaceCleanupCandidate()

目的：
  只看肉眼覺得髒的暗表面：
    floor
    gikPanel

  再拆亮點來源：
    floor
    gikPanel
    ceiling
    wall
    object

v16 來源拆解條件：
  pageUrl = http://127.0.0.1:9004/Home_Studio.html
  scriptSrc = js/Home_Studio.js?v=r6-3-cloud-dark-visible-source-v16
  currentPanelConfig = 3
  currentCameraPreset = cam1
  targetSamples = 4

共同可見表面門檻：
  floor = 0.6116075
  gikPanel = 0.1231235

來源拆解結果：
  floor 可見面：
    dominantSourceSurface = ceiling
    ceiling absoluteHotspotPixelDensity = 0.015659
    wall absoluteHotspotPixelDensity = 0.007882

  gikPanel 可見面：
    dominantSourceSurface = ceiling
    ceiling absoluteHotspotPixelDensity = 0.016336
    wall absoluteHotspotPixelDensity = 0.01324
    gikPanel self absoluteHotspotPixelDensity = 0.012799

白話判讀：
  使用者的猜想成立。
  天花板本身看起來正常，但它作為反彈來源時，會在 GIK / 地板這些暗表面上製造刺眼尖峰。
  牆面是第二來源。

候選清理：
  setCloudDarkSurfaceCleanupCandidate(1, 1.0)

清理範圍：
  只壓 visible floor / GIK。
  只壓 source ceiling / wall。
  預設關閉，正常 render mode 不會自動改變。

候選量測：
  reportCloudDarkSurfaceCleanupCandidateAfterSamples(4, 120000, 1.0)

結果：
  floor:
    hotspotDensityReductionRatio = 0
    p99ReductionRatio = 0.961192
    maxReductionRatio = 0.927955

  gikPanel:
    hotspotDensityReductionRatio = 0
    p99ReductionRatio = 0.837895
    maxReductionRatio = 0.968056

  ceiling / wall / object:
    p99ReductionRatio = 0
    maxReductionRatio = 0

白話判讀：
  候選清理沒有減少亮點顆數。
  但它大幅降低 GIK / 地板亮點的尖銳程度。
  天花板、牆面、物件可見表面沒有被這個候選影響。

低 SPP 圖面驗證：
  已輸出 16 SPP 截圖：
    /private/tmp/home_studio_cleanup_off_16spp.png
    /private/tmp/home_studio_cleanup_on_16spp.png

下一步：
  需要使用者肉眼看 8 / 16 / 48 SPP A/B。
  若肉眼接受，再做 256 / 1024 SPP 高採樣保護，確認不會讓暗表面變髒、變灰或變假。
```

### v17 亮樣本覆蓋率 probe

```
新增 helper：
  reportCloudBrightSampleCoverageProbeAfterSamples()

目的：
  直接量 floor / GIK 低 SPP 時，已抽到正常 Cloud NEE 亮度的可見樣本比例。
  同時量還在等亮度補齊的比例。

實頁條件：
  pageUrl = http://127.0.0.1:9005/Home_Studio.html
  scriptSrc = js/Home_Studio.js?v=r6-3-cloud-bright-sample-coverage-v17
  currentPanelConfig = 3
  currentCameraPreset = cam1
  targetSamples = 4

實頁結果：
  floor + GIK:
    visiblePixelSamples = 1020352
    medianBrightPixelCount = 89664
    medianBrightCoverage = 0.087876
    darkWaitingShareAtMedian = 0.912124
    strongBrightCoverage = 0.017833
    darkWaitingShareAtStrong = 0.982167

  floor:
    visiblePixelSamples = 384696
    contributionEventDensity = 0.128579
    normalBrightThreshold = 0.06116075
    medianBrightCoverage = 0.064612
    darkWaitingShareAtMedian = 0.935388
    maxToP50Ratio = 1341.725044
    coverageVerdict = coverageInsufficient

  gikPanel:
    visiblePixelSamples = 635656
    contributionEventDensity = 0.203406
    normalBrightThreshold = 0.01231235
    medianBrightCoverage = 0.101955
    darkWaitingShareAtMedian = 0.898045
    maxToP50Ratio = 4747.036106
    coverageVerdict = coverageInsufficient

白話判讀：
  floor / GIK 可見樣本很多，但抽到一般亮度的比例很低。
  合計只有約 8.8% 的可見樣本拿到同片表面的一般 Cloud NEE 亮度。
  約 91.2% 的可見樣本仍在等亮度補齊。
  這支持「低 SPP 早期暗像素太多」這條路線。
  v16 硬壓亮尾端會破壞材質觀感，所以接下來改做補暗，不做硬壓亮點。

下一步：
  設計 guarded same-surface dark-fill candidate。
  條件：
    只作用於 floor / GIK。
    只借同片表面、相似法線、相似材質、近距離像素。
    低 SPP 作用較強，SPP 增加後退場。
    預設關閉，先用 A/B toggle 驗證。
```

### v18a 同表面補暗候選

```
新增 helper：
  setCloudSameSurfaceDarkFillCandidate()
  reportCloudSameSurfaceDarkFillCandidateAfterSamples()

目的：
  把 floor / GIK 上已經有 Cloud NEE 事件、但亮度仍低於同片表面一般亮度的樣本，往該表面的一般亮度補一點。
  候選預設關閉。
  只在低 SPP 早期作用，SPP 增加後退場。

守門：
  visible surface 只限 floor / GIK。
  只作用 diffuse bounce 後的 Cloud NEE contribution。
  使用 sampleCounter fade：
    strength = 1.0
    maxSamples = 64
  補光目標：
    baseline measured p50 * 1.25

實頁條件：
  pageUrl = http://127.0.0.1:9005/Home_Studio.html
  scriptSrc = js/Home_Studio.js?v=r6-3-cloud-same-surface-dark-fill-v18a
  currentPanelConfig = 3
  currentCameraPreset = cam1
  targetSamples = 4
  resultJson = /private/tmp/home_studio_same_surface_dark_fill_c3_v18a_result.json

實頁結果：
  medianBrightCoverageLiftAverage = 0.029199
  candidatePassesFirstMetric = true

  floor:
    baselineMedianBrightCoverage = 0.064612
    candidateMedianBrightCoverageAtBaselineThreshold = 0.094298
    medianBrightCoverageLift = 0.029686
    darkWaitingShareReductionRatio = 0.031737
    candidateMaxToP50Ratio = 1080.417537

  gikPanel:
    baselineMedianBrightCoverage = 0.101955
    candidateMedianBrightCoverageAtBaselineThreshold = 0.130668
    medianBrightCoverageLift = 0.028713
    darkWaitingShareReductionRatio = 0.031973
    candidateMaxToP50Ratio = 3822.527385

白話判讀：
  v18a 有把 floor / GIK 的一般亮度覆蓋率往上推。
  幅度偏保守，平均多約 2.9 個百分點。
  暗等待比例約下降 3.2%。
  maxToP50Ratio 也下降，表示亮尾端相對一般亮度沒有那麼誇張。
  使用者肉眼回報：
    8 SPP 以下好像有改善。
    更高 SPP 差異不大。
  這符合候選設計：早期幫忙，後面退場或影響變小。
  目前它只是第一版候選，還不能當正式答案。

下一步：
  依使用者後續肉眼判斷，v18a 退場太快。
  下一輪改成 64 SPP 前持續有效，64 SPP 後再平順退場。
```

### R6-3 Phase2 v18b Cloud same-surface dark-fill 速率曲線修正

```
日期：
  2026-05-06

使用者回報：
  v18a 在 8 SPP 以下好像有改善。
  更高 SPP 差異不大。
  目前退場太快。
  肉眼來看，64 SPP 前都要有作用會比較好。

舊曲線 v18a：
  maxSamples = 64 時，從第 1 SPP 開始線性退場。
  1 SPP  = 100.0%
  8 SPP  = 89.1%
  16 SPP = 76.6%
  32 SPP = 51.6%
  48 SPP = 26.6%
  64 SPP = 1.6%
  65 SPP 後 = 0%

判讀：
  這會讓 32 ~ 64 SPP 的補暗力道太弱。
  使用者看到 8 SPP 以下有改善、後面差異變小，和曲線數字吻合。

新曲線 v18b：
  maxSamples = 64 改成保留期。
  1 ~ 64 SPP 維持 100%。
  64 ~ 128 SPP 使用 smoothstep 平順退場。
  128 SPP 後關閉。

v18b 參考數字：
  1 SPP  = 100.0%
  8 SPP  = 100.0%
  16 SPP = 100.0%
  32 SPP = 100.0%
  48 SPP = 100.0%
  64 SPP = 100.0%
  80 SPP = 84.4%
  96 SPP = 50.0%
  112 SPP = 15.6%
  128 SPP = 0%

改動檔案：
  shaders/Home_Studio_Fragment.glsl
  js/Home_Studio.js
  Home_Studio.html
  docs/tests/r6-3-cloud-mis-weight-probe.test.js

版本：
  r6-3-phase2-cloud-same-surface-dark-fill-v18b

實頁低 SPP 探針：
  pageUrl = http://127.0.0.1:9005/Home_Studio.html?probe=v18b-candidate
  currentPanelConfig = 3
  targetSamples = 4
  resultJson = /private/tmp/home_studio_same_surface_dark_fill_c3_v18b_result.json
  medianBrightCoverageLiftAverage = 0.029199
  candidatePassesFirstMetric = true

  floor:
    baselineMedianBrightCoverage = 0.064612
    candidateMedianBrightCoverageAtBaselineThreshold = 0.094298
    medianBrightCoverageLift = 0.029686
    darkWaitingShareReductionRatio = 0.031737

  gikPanel:
    baselineMedianBrightCoverage = 0.101955
    candidateMedianBrightCoverageAtBaselineThreshold = 0.130668
    medianBrightCoverageLift = 0.028713
    darkWaitingShareReductionRatio = 0.031973

探針判讀：
  4 SPP 位於 v18a 與 v18b 的強作用區，數字相同屬於合理結果。
  v18b 的主要差異在 32 ~ 64 SPP，需用肉眼比較確認。

驗收重點：
  開啟候選後，1 ~ 64 SPP 應該都能看見補暗效果。
  96 SPP 附近效果應該開始明顯變弱。
  128 SPP 後應該回到保護材質與收斂結果優先。

使用者截圖回報：
  檔案：
    /Users/eajrockmacmini/Downloads/260506-cam1-default-4spp (on).png
    /Users/eajrockmacmini/Downloads/260506-cam1-default-4spp (off).png
  使用者判斷：
    有開與沒開看起來完全一樣。
    先前覺得有改善屬於心理作用。

截圖差異量測：
  尺寸：
    2560 x 1440
  full mean_abs_rgb:
    R = 15.0672
    G = 14.9132
    B = 13.8369
  full luma:
    on = 91.1775
    off = 97.5957
    delta = -6.4182
  center_panels luma:
    on = 71.5918
    off = 78.0030
    delta = -6.4112
  right_gik luma:
    on = 81.6637
    off = 88.3503
    delta = -6.6867

修正判讀：
  v18b 的診斷數字沒有轉成主畫面可見差異。
  目前應把 same-surface dark-fill 候選視為視覺 no-go。
  先前 probe 看到的是 Cloud contribution 診斷通道覆蓋率，不等於一般 render 的肉眼改善。

新結論：
  暫停沿 v18b 調 strength 或退場曲線。
  下一步需要改成主畫面 screen-delta probe，或改查 4 SPP 主要噪點來源。
```

### R6-3 Phase2 1 SPP 黑點主因重查

```
日期：
  2026-05-06

使用者判斷：
  最該查的是 1 SPP。
  因為黑點最多，等於每次移動都要被閃一次黑幕。

實頁條件：
  pageUrl = http://127.0.0.1:9005/Home_Studio.html?probe=screen-dark-1spp
  currentPanelConfig = 3
  currentCameraPreset = cam1
  scriptSrc = js/Home_Studio.js?v=r6-3-cloud-same-surface-dark-fill-v18b
  shaderFile = Home_Studio_Fragment.glsl?v=r6-3-cloud-same-surface-dark-fill-v18b
  cloudSameSurfaceDarkFillMode = 0
  cloudMisWeightProbeMode = 0
  activeLightIndex = [7, 8, 9, 10]

截圖輸出：
  /private/tmp/home_studio_1spp_dark_probe/cam1-c3-1spp.png
  /private/tmp/home_studio_1spp_dark_probe/cam1-c3-2spp.png
  /private/tmp/home_studio_1spp_dark_probe/cam1-c3-4spp.png
  /private/tmp/home_studio_1spp_dark_probe/cam1-c3-8spp.png
  /private/tmp/home_studio_1spp_dark_probe/cam1-c3-16spp.png

局部黑洞指標：
  定義：
    local45_min30 = 周邊平均亮度 >= 30，且該像素亮度低於周邊平均 45%。

  full:
    1 SPP  = 0.010699
    2 SPP  = 0.010047
    4 SPP  = 0.008776
    8 SPP  = 0.006014
    16 SPP = 0.003624

  lower_floor:
    1 SPP  = 0.025070
    2 SPP  = 0.020000
    4 SPP  = 0.017680
    8 SPP  = 0.011690
    16 SPP = 0.006743

  right_gik:
    1 SPP  = 0.018313
    2 SPP  = 0.013280
    4 SPP  = 0.013015
    8 SPP  = 0.010382
    16 SPP = 0.007933

Cloud bright sample coverage 1 SPP：
  resultJson = /private/tmp/home_studio_coverage_probe_c3_1spp_result.json
  floorGikMedianBrightCoverage = 0.087876
  floorGikDarkWaitingShareAtMedian = 0.912124
  coverageInsufficientSurfaces = [floor, gikPanel]

  floor:
    visiblePixelSamples = 96174
    contributionEventDensity = 0.128579
    medianBrightCoverage = 0.064612
    darkWaitingShareAtMedian = 0.935388
    maxToP50Ratio = 1341.725044

  gikPanel:
    visiblePixelSamples = 158914
    contributionEventDensity = 0.203406
    medianBrightCoverage = 0.101955
    darkWaitingShareAtMedian = 0.898045
    maxToP50Ratio = 4747.036106

判讀：
  使用者判斷正確，1 SPP 是最該先解的痛點。
  第一張主畫面的黑點集中在 lower_floor、right_gik、深色物件區。
  floor / GIK 約九成可見像素在 1 SPP 還沒達到一般亮度。
  same-surface dark-fill 只改已經抽到 Cloud contribution 的樣本，無法補「第一張沒有抽到有效亮度」的像素。

新路線：
  停止沿 v18b 補暗候選微調。
  下一步改查 1 SPP first-frame strategy：
    1. 主畫面 screen-delta / local dark-hole probe 作為新指標。
    2. 優先研究移動後前 1 ~ 4 SPP 的顯示端保護，例如短暫沿用上一張穩定畫面、或只對局部黑洞做早期視覺修補。
    3. 若要動採樣端，目標要能增加第一張 floor/GIK 有效 coverage，而不是只提高已命中的 contribution 亮度。
```

### v10 判讀保留

```
使用者驗收：
  await reportCloudDirectNeeScreenBandProbeAfterSamples(64, 120000)

top band：
  eventShare = 0.262761
  contributionShare = 0.593603
  averageContributionLuma = 1.226663
  averageContributionLift = 2.259101

bottom band：
  eventShare = 0.213587
  contributionShare = 0.087294
  averageContributionLuma = 0.2219211

白話判讀：
  燈具在上方，上方比較亮本身合理。
  v10 的價值是確認 top band 的加權亮度貢獻高度集中。
  目前要追的是：
    A. top band 是否由少數超亮樣本主導。
    B. Cloud direct NEE 來自 primary-surface 或 bounced-surface。
```

### 最高 ROI 下一步

```
1. 守門式同表面補暗候選
   目的：
     用同片 GIK / 地板上已抽到正常亮光的像素，補給附近仍偏暗的像素。
     只在低 SPP 早期加速亮度補齊。

   守門：
     同片表面。
     相似法線。
     相似材質。
     近距離。
     SPP 增加後退場。

2. 低 / 高 SPP 驗證
   看點：
     8 / 16 / 48 spp 低採樣髒感是否下降。
     256 / 1024 spp 材質光澤與最終亮度是否維持。
```

### 需要避開的地雷

```
1. 不使用 v7 前 BSDF-hit 舊讀值。
   理由：當時 probe readback 混入非 Cloud 終端顏色。

2. 不重試 Phase 1A / 1B target-shape 類路線。
   理由：使用者已肉眼 no-go。

3. 不把「上方比較亮」本身列為 bug。
   理由：燈具本來就在上方，物理上合理。

4. 不採用 v16 hard clamp cleanup candidate。
   理由：使用者肉眼 no-go；GIK 變土色、地板變霧面，破壞材質該有反光。

5. 不先上 SVGF / OIDN 掩蓋現象。
   理由：目前已有更直接的 Cloud direct NEE 高 ROI 線索。

6. 不讓 probe 污染 normal render。
   要求：probe-only、normalRenderProbeMode = 0、改 shader / JS 時同步 cache-bust。

7. 不把自然 BSDF-hit 當主嫌。
   理由：v9 到 64 samples 仍是 0 event；保留為低 ROI 備線。
```

### 未來實驗路徑

```
Step A：top band hotspot / percentile probe ✅
  產出：
    top band 的 contribution p50 / p90 / p99 / max。

Step B：primary vs bounced split ✅
  產出：
    primary-surface Cloud NEE 與 bounced-surface Cloud NEE 比例。
    v12 結果：bounced-surface = 100%，split/all = 1。

Step C：bounced direct NEE surface-class probe ✅
  產出：
    ceiling 約 63.6%，wall 約 34.4%，floor + GIK 約 0.54%。

Step D：visible surface hotspot / dark source probe ✅
  產出：
    v15：GIK 平常偏暗，亮點一冒出來特別刺眼。
    v16：floor / GIK 暗面尖峰主要來自 ceiling，其次來自 wall。
    v16 cleanup candidate：肉眼 no-go，不能採用。

Step E：亮樣本覆蓋率 probe ✅
  產出：
    floor + GIK 一般亮度覆蓋率 0.087876。
    同片表面仍等待亮度補齊比例 0.912124。

Step F：安全補暗候選 ✅ 初測完成
  產出：
    v18a 同表面補暗候選。
    floor / GIK coverage 平均提升 0.029199。
    暗等待比例約降 3.2%。
    用同片、相似法線、相似材質、低 SPP 退場概念補給弱亮度樣本。

Step G：低 / 高 SPP 肉眼保護 ← 下一步
  產出：
    8 / 16 / 48 spp 看早期髒感。
    256 / 1024 spp 看材質光澤與最終亮度。
    低 SPP 作用較強，SPP 變高後退場。
```

### 後處理候補

```
SVGF / reproject / OIDN 仍保留，但目前順位下降。

啟動條件：
  top hotspot 與 primary/bounced split 量完後，
  若 Cloud direct NEE 採樣微調仍無法改善 30 ~ 60 秒目標，
  再回到後處理候補。
```

### 觸發 R7 條件（往上跳）

```
若 R6-3 probe 與最小 A/B 顯示：
  細長 Cloud 燈條的高變異需要 ReSTIR / path guiding 等級的採樣重構。
  R6-3 的局部採樣調整與後處理候補都達不到 30 ~ 60 秒目標。

→ 啟動 R7「採樣演算法升級」。
→ 詳見 docs/SOP/R7：採樣演算法升級.md。
```

### 最新紀錄位置

```
完整流水帳：
  docs/SOP/Debug_Log.md

最新段落：
  Cloud visibility probe v4/v5 亮度回歸教訓（2026-05-03）
  Cloud MIS weight probe（2026-05-04）
  BSDF-hit terminal isolation v7 追查（2026-05-05）
  Forced BSDF-hit probe v8b（2026-05-05）
  Natural BSDF-hit frequency probe v9（2026-05-05）
  Direct NEE screen-band probe v10（2026-05-05）
  Direct NEE screen-band probe v10 使用者驗收補記（2026-05-05）
  Direct NEE top-band percentile probe v11（2026-05-05）
  Direct NEE diffuseCount split probe v12（2026-05-05）
  Bounced direct NEE floor/GIK 與 receiver-class probe v13/v14（2026-05-05）
  Visible surface hotspot probe v15（2026-05-06）
  Dark visible-surface source probe / cleanup candidate v16（2026-05-06）
  v16 cleanup 使用者肉眼 no-go 與亮樣本覆蓋率新方向（2026-05-06）
  1 SPP 主畫面黑點重查與 v19 first-frame burst（2026-05-06）
  v20 movement protection SOP 與首輪實作（2026-05-06）

實作入口：
  js/Home_Studio.js
    CLOUD_MIS_WEIGHT_PROBE_VERSION
    reportCloudDirectNeeScreenBandProbeAfterSamples()
    reportCloudDirectNeeTopBandPercentileProbeAfterSamples()
    reportCloudDirectNeeDiffuseCountSplitProbeAfterSamples()
    reportCloudBouncedDirectNeeFloorGikProbeAfterSamples()
    reportCloudBouncedDirectNeeReceiverClassProbeAfterSamples()
    reportCloudVisibleSurfaceHotspotProbeAfterSamples()
    reportCloudDarkVisibleSurfaceHotspotSourceProbeAfterSamples()
    reportCloudDarkSurfaceCleanupCandidateAfterSamples()
    setCloudDarkSurfaceCleanupCandidate()
    setFirstFrameRecoveryConfig()
    reportFirstFrameRecoveryConfig()
    setMovementProtectionConfig()
    reportMovementProtectionConfig()
    reportNaturalCloudBsdfHitFrequencyAfterSamples()
    reportForcedCloudBsdfHitProbeAfterSamples()

測試契約：
  docs/tests/r6-3-cloud-mis-weight-probe.test.js
  docs/tests/r6-3-cloud-sampling-budget-diagnostic.test.js
  docs/tests/r6-3-cloud-visibility-probe.test.js
  docs/tests/r6-3-cloud-facing-diagnostic.test.js
  docs/tests/r6-3-v20-movement-protection.test.js
```

---

## R6-3 Phase2 v19：First-frame burst（2026-05-06）

使用者指出真正痛點在 1 SPP：每次移動後都會先看到一張黑點很多的畫面。前一條 v18b same-surface dark-fill 已被使用者肉眼判定 no-go，因此本段改走 first-frame 治療。

```
實作版本：
  r6-3-phase2-first-frame-burst-v19

改動：
  js/InitCommon.js
    firstFrameRecoveryEnabled = true
    firstFrameRecoveryTargetSamples = 4
    firstFrameRecoveryClearWhileMoving = true
    setFirstFrameRecoveryConfig()
    reportFirstFrameRecoveryConfig()

  Home_Studio.html
    InitCommon.js?v=r6-3-first-frame-burst-v19
    Home_Studio.js?v=r6-3-first-frame-burst-v19

  js/Home_Studio.js
    CLOUD_MIS_WEIGHT_PROBE_VERSION = r6-3-phase2-first-frame-burst-v19
    Home_Studio_Fragment.glsl?v=r6-3-first-frame-burst-v19

策略：
  第一張可見畫面不直接停在 1 SPP。
  render loop 在同一個畫面內先跑到 4 SPP，再送到螢幕。
  移動中預設先清掉舊累積，再用目前視角跑 4 次，避免舊角度混進來。

Console 控制：
  reportFirstFrameRecoveryConfig()
  setFirstFrameRecoveryConfig({ enabled: false })
  setFirstFrameRecoveryConfig({ enabled: true, targetSamples: 4, clearWhileMoving: true })
```

v19 headless Brave / CDP 量測：

```
網址：
  http://127.0.0.1:9005/Home_Studio.html?probe=v19-first-frame-on

場景：
  C3
  cam1
  same-surface dark-fill off
  Cloud MIS probe off

截圖：
  /private/tmp/home_studio_v19_first_frame_probe_on/cam1-c3-on-1spp.png
  /private/tmp/home_studio_v19_first_frame_probe_on/cam1-c3-on-2spp.png
  /private/tmp/home_studio_v19_first_frame_probe_on/cam1-c3-on-4spp.png
  /private/tmp/home_studio_v19_first_frame_probe_on/cam1-c3-on-8spp.png
  /private/tmp/home_studio_v19_first_frame_probe_on/cam1-c3-on-16spp.png

summary:
  /private/tmp/home_studio_v19_first_frame_probe_on/capture-summary.json

關鍵結果：
  requestedSamples: 1
  actual samples: 4
  firstFrameRecovery.lastPassCount: 4
  firstFrameRecovery.targetSamples: 4
  firstFrameRecovery.enabled: true

first visible frame metrics:
  full veryDarkRatio: 0.023590874
  lower_floor veryDarkRatio: 0.062237687
  right_gik veryDarkRatio: 0.063606532
  full meanLuma: 121.279218

對照：
  舊 1 SPP 仍會呈現大量黑點。
  v19 第一張已跳過純 1 SPP 顯示，改成 4 SPP 的第一眼。
```

判讀：

```
已改善：
  移動後第一張不再直接把 1 SPP 顯示給使用者。
  黑幕感應該會明顯下降。

尚未改善：
  白點與黑點噪聲仍然存在。
  v19 是 first-frame 顯示節奏治療，不是降噪器。
  移動中會多跑取樣，GPU 成本會提高；需使用者用肉眼與互動感驗收。

下一步：
  A. 使用者先用 cam1 / C3 移動視角，檢查是否少掉黑幕閃一下。
  B. 若 FPS 太低，先把 targetSamples 測 2 或 3。
  C. 若黑幕消失但噪點仍刺眼，再做局部 dark-hole repair 或 history hold 的小範圍 A/B。
```

v19a 快照干擾排除：

```
使用者回報：
  first-frame burst 確實不黑。
  但第一張延後顯示造成卡手感。
  自動 SPP 快照也會造成卡頓，會干擾真實手感判斷。

改動：
  保留快照功能。
  預設關閉快照。
  新增快照開關：
    Home_Studio.html
      btn-toggle-snapshots

    js/Home_Studio.js
      SNAPSHOT_CAPTURE_ENABLED = false
      SNAPSHOT_MILESTONES = []
      SNAPSHOT_MILESTONE_PRESET 保留原本節點
      setSnapshotCaptureEnabled(enabled)
      window.setSnapshotCaptureEnabled = setSnapshotCaptureEnabled

行為：
  預設不自動存 SPP 快照。
  預設手動存圖與打包下載按鈕停用。
  按「快照：關」切到「快照：開」後，手動存圖與自動節點快照恢復。

驗收重點：
  先在快照關閉狀態測互動手感。
  若仍卡，主要嫌疑回到 first-frame burst 自身。
```

v19a 使用者驗收補記：

```
使用者回報：
  快照關閉後很順。
  4 SPP first-frame burst 讓 1 SPP 黑幕消失。
  但 4 SPP 畫面仍然很髒，移動時視線仍被噪點阻礙。

判讀：
  快照卡頓已排除，不再是目前手感主因。
  first-frame burst 解的是黑幕，不是移動期間可視性。
  單純把 targetSamples 往 8 / 16 拉，會增加等待與卡手感，不是優先路線。

下一步方向：
  A. 優先研究 movement visibility protection。
  B. 候選一：移動期間短暫沿用上一張穩定畫面，再淡入新累積。
  C. 候選二：只在移動期做局部 dark-hole / firefly 緩和。
  D. 先建立可關閉 A/B 開關，避免影響靜止收斂。
```

---

## R6-3 Phase2 v20：Movement Protection（2026-05-06）

SOP 位置：

```
docs/SOP/R6-3-v20：movement protection.md
```

首輪範圍：

```
完成：
  1. 總開關與量測框架
  2. 保存上一張穩定畫面
  3. 移動時混入上一張穩定畫面

後續：
  4. edge / normal 保護
  5. depth / velocity 類判斷
```

實作：

```
js/InitCommon.js
  firstFrameRecoveryTargetSamples = 4
  firstFrameRecoveryMovingTargetSamples = 16
  movementProtectionRenderTarget
  movementProtectionEnabled = true
  movementProtectionMovingBlend = 0.0
  movementProtectionLowSppPreviewStrength = 0.15
  movementProtectionSpatialPreviewStrength = 0.0
  movementProtectionWidePreviewStrength = 0.95
  movementProtectionMinStableSamples = 16
  movementProtectionStableReady = false
  setMovementProtectionConfig()
  reportMovementProtectionConfig()
  captureMovementProtectionStableFrame()
  updateMovementProtectionUniforms()

shaders/ScreenOutput_Fragment.glsl
  tMovementProtectionStableTexture
  uMovementProtectionMode
  uMovementProtectionBlend
  displayColor = mix(displayColor, movementStableColor, movementBlend)

Home_Studio.html
  InitCommon.js?v=r6-3-movement-protection-v21a
```

預設行為：

```
相機靜止且 sampleCounter >= 16：
  保存一張 ScreenOutput 結果到 movementProtectionRenderTarget。

相機移動且 stableReady = true：
  C3 / C4 移動時同一個 visible frame 累到 16SPP。
  預設保留 37 點 wide moving preview。
  history mix 預設關閉，避免舊視角殘影。

相機停止：
  movementProtectionBlend 回到 0。
  path tracing 繼續正常收斂。
```

Console 控制：

```
reportMovementProtectionConfig()
setMovementProtectionConfig({ enabled: false })
setMovementProtectionConfig({ enabled: true, movingBlend: 0.65, minStableSamples: 16 })
setMovementProtectionConfig({ movingBlend: 0.0 })
setMovementProtectionConfig({ movingBlend: 0.45 })
setMovementProtectionConfig({ movingBlend: 0.80 })
setMovementProtectionConfig({ spatialPreviewStrength: 0.90 })
setMovementProtectionConfig({ spatialPreviewStrength: 0.0 })
setMovementProtectionConfig({ widePreviewStrength: 0.95 })
setMovementProtectionConfig({ widePreviewStrength: 0.0 })
setFirstFrameRecoveryConfig({ movingTargetSamples: 16 })
setFirstFrameRecoveryConfig({ movingTargetSamples: 4 })
```

CDP smoke test：

```
網址：
  http://127.0.0.1:9005/Home_Studio.html?probe=v20-movement-protection-cache-smoke

結果：
  movementProtection.version: r6-3-phase2-movement-protection-v20e
  movementProtection.enabled: true
  movementProtection.stableReady: true
  movementProtection.movingBlend: 0.65
  movementProtection.minStableSamples: 16
  movementProtection.lastCaptureSamples: 18

summary:
  /private/tmp/home_studio_v20_movement_probe_cache/capture-summary.json
```

驗收重點：

```
1. 開頁後先等 Samples >= 16。
2. 移動視角，檢查畫面是否比 v19a 更可讀。
3. 預設 movingBlend = 0，確認沒有上一個視角的透明雙影。
4. 需要診斷 history mix 時，再手動設定 movingBlend 0.45 或 0.65。
5. 停止移動後確認畫面會繼續收斂。
```

v20a user no-go 修正：

```
使用者回報：
  v20 開啟後 4 SPP 還是很髒。

根因：
  v20 有保存穩定畫面，但移動清除時把 stableReady 清掉。
  Step 3 又用 first-frame burst 的 activeRenderCameraMoving=false 判斷顯示混合。
  實際移動時 uMovementProtectionBlend 沒有打開。

修正：
  movementProtectionPreserveStableAcrossCameraReset = true。
  移動清除與 first-frame 清除期間保留上一張穩定畫面。
  Step 3 改用實際 cameraIsMoving 決定 movement protection blend。
  停止移動後才更新穩定畫面。

CDP movement-check：
  pageUrl:
    http://127.0.0.1:9005/Home_Studio.html?probe=v20a-movement-protection-active-blend-2

  summary:
    /private/tmp/home_studio_v20a_movement_probe_2/capture-summary.json

  before move:
    stableReady: true
    lastBlend: 0
    lastCaptureSamples: 21

  during move:
    cameraIsMoving: true
    currentSamples: 4
    stableReady: true
    lastBlend: 0.65
```

v20b user no-go 修正：

```
使用者回報：
  配置 1 移動時，4 SPP 變得超暗。

根因：
  v20a 保留穩定畫面的範圍太大。
  配置切換、視角按鈕、燈光池重建、參數變動也沿用上一張穩定畫面。
  移動時混入舊狀態，配置 1 會被舊暗圖壓低亮度。

修正：
  新增 invalidateMovementProtectionStableFrame(reason)。
  下列狀況會清掉舊穩定圖：
    applyPanelConfig
    switchCamera
    rebuildActiveLightLUT
    sceneParamsChanged

保留：
  一般滑鼠移動仍保留穩定圖，維持 4 SPP movement protection。

CDP movement-check：
  pageUrl:
    http://127.0.0.1:9005/Home_Studio.html?probe=v20b-config1-movement-dark-fix

  summary:
    /private/tmp/home_studio_v20b_config1_movement_probe/capture-summary.json

  screenshot:
    /private/tmp/home_studio_v20b_config1_movement_probe/cam1-c3-on-18spp.png

  during move:
    config: 1
    cameraIsMoving: true
    currentSamples: 4
    stableReady: true
    lastBlend: 0.65
    meanLuma(full): 141.035252
    veryDarkRatio(full): 0
```

v20c user no-go 修正：

```
使用者回報：
  v20b 後，配置 1 移動時 4 SPP 仍然超暗。

修正：
  movementProtectionConfigAllowed() 只允許配置 3 / 4。
  配置 1 / 2 移動時 uMovementProtectionBlend 固定為 0。
  reportMovementProtectionConfig() 新增 configAllowed。

CDP movement-check：
  pageUrl:
    http://127.0.0.1:9005/Home_Studio.html?probe=v20c-config1-gate-check

  summary:
    /private/tmp/home_studio_v20c_config1_gate_probe/capture-summary.json

  during move:
    configAllowed: false
    lastBlend: 0
    currentSamples: 4
    meanLuma(full): 140.744454
    veryDarkRatio(full): 0
```

v20d user no-go 修正：

```
使用者回報：
  C3 / C4 也是 4 SPP 超暗，不能只排除 C1 / C2。

根因：
  C3 / C4 切換後立刻移動時，還沒有 Samples >= 16 的穩定畫面。
  history mix 無圖可混，最後仍顯示原始 4 SPP 暗畫面。

修正：
  C3 / C4 移動中新增 low-SPP preview fallback。
  stableReady = false 時，也會用 display-space preview curve 提亮可視性。
  stableReady = true 時，仍保留原 history mix。
  C1 / C2 繼續不跑 movement protection。

驗證：
  C3 / 4SPP / movement-check:
    summary: /private/tmp/home_studio_v20d_c3_4spp_preview_probe/capture-summary.json
    lastPreviewStrength: 0.55
    meanLuma(full): 95.351693 -> 110.437023
    veryDarkRatio(full): 0.082047 -> 0.016100

  C4 / 4SPP / movement-check:
    summary: /private/tmp/home_studio_v20d_c4_4spp_preview_probe/capture-summary.json
    lastPreviewStrength: 0.55
    meanLuma(full): 87.920790 -> 104.796660
    veryDarkRatio(full): 0.053099 -> 0.009472
```

v20f user no-go 修正：

```
使用者回報：
  C3 4SPP 不會暗畫面了，但是有殘影問題。

根因：
  v20e 修掉 ScreenOutput shader 編譯錯誤後，history mix 開始真的作用。
  movementProtectionMovingBlend 預設 0.65 會把上一張穩定畫面混進移動畫面。
  使用者看到的雙影，就是舊視角與目前視角重疊。

修正：
  movementProtectionMovingBlend 預設改為 0.0。
  C3 / C4 low-SPP preview fallback 保持開啟。
  history mix 保留 Console 手動 A/B 能力。
  InitCommon / ScreenOutput cache token bump 到 v20f。

驗收：
  reportMovementProtectionConfig()
    version: r6-3-phase2-movement-protection-v20f
    movingBlend: 0
    lastBlend: 0

  C3 / C4 4SPP 移動時：
    不再出現上一個視角的透明雙影。
    亮度維持 v20e 修正後的可視狀態。
```

v20g user no-go 修正：

```
使用者回報：
  v20f 不會有殘影了，但 C3 4SPP 跟今天一開始相比沒有明顯差別。

根因：
  v20f 的 low-SPP display lift 只會提亮畫面。
  4SPP 的主要遮擋來自滿畫面的高頻亮暗雜點。
  提亮會讓白點一起變亮，乾淨度沒有實質改善。

修正：
  movementProtectionLowSppPreviewStrength 降為 0.35。
  movementProtectionSpatialPreviewStrength 預設 0.90。
  C3 / C4 移動中，ScreenOutput 在 tone mapping 前做 spatial moving preview。
  用 13 點局部平均建立 movementSpatialPreviewHdr。
  用 movementBrightLimit 壓回過亮 speckle。
  對過暗點做少量 local lift。
  path tracing accumulation 不讀 preview 結果。
  InitCommon / ScreenOutput cache token bump 到 v20g。

驗收：
  reportMovementProtectionConfig()
    version: r6-3-phase2-movement-protection-v20g
    movingBlend: 0
    lowSppPreviewStrength: 0.35
    spatialPreviewStrength: 0.90

  C3 / C4 4SPP 移動時：
    不應出現上一個視角的透明雙影。
    白色亮點密度與黑點突兀感應低於 v20f。
    停止移動後回到正常 path tracing 收斂。
```

v20h user no-go 修正：

```
使用者回報：
  v20g 還是超髒。

根因：
  v20g 的 13 點局部清理太弱。
  C3 / C4 4SPP 問題是整片樣本圖樣遮住視線。
  少量 speckle 壓制無法讓移動時畫面變得可讀。

修正：
  v20g spatial preview 預設關閉。
  movementProtectionLowSppPreviewStrength 降到 0.15。
  movementProtectionWidePreviewStrength 預設 0.95。
  ScreenOutput 新增 37 點 wide moving preview。
  wide preview 只吃目前這一幀，不吃舊視角。
  過亮中心點先用 movementWideBrightLimit 壓回局部範圍。
  InitCommon / ScreenOutput cache token bump 到 v20h。

驗收：
  reportMovementProtectionConfig()
    version: r6-3-phase2-movement-protection-v20h
    movingBlend: 0
    lowSppPreviewStrength: 0.15
    spatialPreviewStrength: 0
    widePreviewStrength: 0.95

  C3 / C4 4SPP 移動時：
    允許畫面比 v20g 模糊。
    噪點遮擋應低於 v20g。
    不應出現上一個視角的透明雙影。
```

v21a user no-go 修正：

```
使用者回報：
  v20h 還是一樣很髒，不再截圖。

根因：
  v20 到 v20h 都在處理同一張 4SPP 畫面。
  後製清理無法補足樣本不足。
  C3 / C4 移動時真正的當前畫面仍只有 4SPP。

修正：
  保留 firstFrameRecoveryTargetSamples = 4。
  新增 firstFrameRecoveryMovingTargetSamples = 16。
  C3 / C4 且 cameraIsMoving 時，把 visible frame 內部 pass target 拉到 16。
  history mix 繼續維持 movingBlend = 0。
  InitCommon / ScreenOutput cache token bump 到 v21a。

驗收：
  reportFirstFrameRecoveryConfig()
    targetSamples: 4
    movingTargetSamples: 16

  C3 / C4 移動時：
    左下 Samples 應顯示 16。
    噪點遮擋應低於 v20h。
    FPS 可能下降。
```

v22a movement preview 改向：

```
使用者回報：
  v21a 讓 FPS 掉到 3，畫面超模糊又卡手。
  使用者判定 16SPP visible frame no-go。

根因：
  低 SPP 髒訊號來自隨機 path tracing 樣本不足。
  顯示端抹平只會換成模糊。
  同一 visible frame 拉高樣本數會犧牲手感。

修正：
  新增 uMovementPreviewMode。
  C3 / C4 移動中改走 CalculateMovementPreview。
  CalculateMovementPreview 只跑一次 SceneIntersect，使用第一個命中點與 deterministic preview light。
  PathTracingCommon 在 uMovementPreviewMode 開啟時關閉 pixel jitter、aperture jitter、previousTexture history。
  firstFrameRecoveryMovingTargetSamples 改為 1。
  lowSppPreviewStrength / spatialPreviewStrength / widePreviewStrength 預設 0。
  movement preview 期間跳過 borrow pass。
  InitCommon / Home_Studio / ScreenOutput cache token bump 到 v22a。

驗收：
  reportMovementProtectionConfig()
    version: r6-3-phase2-movement-protection-v22a
    movementPreviewEnabled: true
    uniformMovementPreviewMode: moving 時為 1
    lowSppPreviewStrength: 0
    spatialPreviewStrength: 0
    widePreviewStrength: 0

  reportFirstFrameRecoveryConfig()
    targetSamples: 4
    movingTargetSamples: 1

  C3 / C4 移動時：
    FPS 不應再像 v21a 掉到 3。
    移動中應優先乾淨與順手。
    停止後回原 path tracing 收斂。
```

v22b cheap-preview no-go 回撤：

```
使用者回報：
  v22a 移動時一直閃出灰色廉價建模。
  此 UX no-go。

根因：
  CalculateMovementPreview 是一次命中的簡化預覽。
  它雖然順，但會犧牲正式材質、紋理與間接光觀感。

修正：
  movementPreviewEnabled 預設改 false。
  uMovementPreviewMode 預設維持 0。
  CalculateMovementPreview 保留為 Console 診斷工具，不作預設移動畫面。
  InitCommon / Home_Studio / ScreenOutput cache token bump 到 v22c。

驗收：
  reportMovementProtectionConfig()
    version: r6-3-phase2-movement-protection-v22c
    movementPreviewEnabled: false
    moving 時 uniformMovementPreviewMode: 0

  C3 / C4 移動時：
    不再閃出灰色簡化模型。
    後續研究改找不破壞正式材質的低 SPP 路線。
```

v22c C1 / C2 FPS 回復：

```
使用者回報：
  setFirstFrameRecoveryConfig({ targetSamples: 1 }) 後，C1 / C2 變順。

根因：
  first-frame recovery 的 4SPP target 原本套在所有配置。
  C1 / C2 沒有 C3 / C4 movement protection，但仍被 first-frame recovery 多跑樣本。

修正：
  新增 firstFrameRecoveryConfigTargetSamples(activeCameraMoving)。
  C1 / C2 active target = 1。
  C3 / C4 active target = firstFrameRecoveryTargetSamples，也就是 4。
  reportFirstFrameRecoveryConfig() 新增 configTargetSamples。
  InitCommon / Home_Studio / ScreenOutput cache token bump 到 v22c。

驗收：
  C1 / C2：
    reportFirstFrameRecoveryConfig().configTargetSamples = 1

  C3 / C4：
    reportFirstFrameRecoveryConfig().configTargetSamples = 4
```

v22d C3 / C4 移動輕量 2SPP：

```
使用者決策：
  試選項 4，移除選項 1。

修正：
  C1 / C2 active target 維持 1。
  C3 / C4 移動時 active target 改 2。
  C3 / C4 停止後 refill active target 回 4。
  C3 / C4 移動期間跳過 borrow pass。
  reportMovementProtectionConfig() 新增 lowCostMovingActive。
  InitCommon / Home_Studio / ScreenOutput cache token bump 到 v22d。

驗收：
  C1 / C2：
    reportFirstFrameRecoveryConfig().configTargetSamples = 1

  C3 / C4 移動時：
    reportFirstFrameRecoveryConfig().configTargetSamples = 2
    reportMovementProtectionConfig().lowCostMovingActive = true

  C3 / C4 停止後：
    reportFirstFrameRecoveryConfig().configTargetSamples = 4
```

v20e compile fix：

```
使用者回報：
  Console 出現 ScreenOutput fragment shader compile error。

根因：
  ScreenOutput_Fragment.glsl 呼叫 Three 注入的 ReinhardToneMapping(filteredPixelColor)。
  目前 three 版本注入函式不接受 vec3。

修正：
  新增 HomeStudioReinhardToneMap(vec3 color)。
  ScreenOutput 改呼叫本地 vec3 helper。
  InitCommon 與 ScreenOutput cache token bump 到 v20e。

驗證：
  node docs/tests/r6-3-v20-movement-protection.test.js
  node docs/tests/r6-3-cloud-mis-weight-probe.test.js
  node docs/tests/r6-3-max-samples.test.js
  node --check js/InitCommon.js
  node --check js/Home_Studio.js
  git diff --check
```

---

## R7 採樣演算法升級（未來展望）

R6-3 已確認 WebGL path tracing 低 SPP 修補線停止。
下一輪優先順序改為由小到大：

```
1. R7 丁 blue noise sampling 升級
2. R7 丙光源 importance sampling 機率優化
3. WebGPU / hybrid preview 小型概念驗證
4. ReSTIR / path guiding 重做暫緩
```

```
細節大綱     →  docs/SOP/R7：採樣演算法升級.md
```

---

## 歷史結案一覽（一行帶過、想看細節點檔）

```
R6 主線 1 階段 1     雙邊濾波撤回      →  docs/SOP/R6-1：雙邊濾波後處理降噪.md
R6-2 Phase 1.0      現況盤點 ✅       →  .omc/REPORT-R6-2-Phase-1.0.md
R6-2 Phase 1.5      SAH -39% 回滾    →  .omc/REPORT-R6-2-Phase-1.5-Step1-SAH-rollback.md
R6-2 Phase 1.5      leaf ≤ 1%        →  .omc/REPORT-R6-2-Phase-1.5-Step2-step0-noop.md
R6-2 桶 4 F1        frame-skip ✅     →  .omc/REPORT-R6-2-bucket4-F1-conclusion.md
R6-2 桶 4 F2        timer ❌          →  .omc/REPORT-R6-2-bucket4-F2-step0-noop.md
R6-2 桶 2 #2 RR     ralplan APPROVE   →  .omc/plans/R6-2-bucket2-russian-roulette.md（v2 共識）
R5                  Cloud 撤回        →  docs/SOP/R5：Cloud光源重構.md §R5-Z
```

---

## Git 部署狀態

```
main 最新       504b817（R6-3 + R7 SOP 大綱落地、2026-05-01）
                上一筆 bf6f60c（R6 LGG-r30 final、2026-05-01）
origin/main     已同步推上 GitHub
r3-light        本機 + 遠端均刪除（2026-05-01 整理）
工作樹提醒       2026-05-06 有 R6-3 結案整理 / WebGPU 交棒 docs 變更，交接前需重看 git status

紀律
  R 主線整段 DONE 才合 main + push
  R 內子階段完工僅本地 commit
  下一階段直接從 main 開新分支
```

---

## 不在範圍

```
1. R5 整面光板實驗                     已撤回、見 R5：Cloud光源重構.md §R5-Z
2. R6-1 5×5 雙邊濾波                   已撤回、見 R6-1：雙邊濾波後處理降噪.md §R6-1-W
3. 任何「減光源加速降噪」提案           物理本質不行
4. 改 emission 分布 / 改光源端          屬光源端、不屬效能端
5. UI 風格大改                          R4 凍結
6. IES 光形匯入 / 採購型錄整合          商品未提供標準格式
7. 色彩管理校準                          沒實體燈具 reference 可比對
8. 飛利明Varilumi 燈具採購              使用者自理、屬硬體決策
```

---

## Debug 通用紀律

任何 R 階段動工前必讀：`docs/SOP/Debug_Log.md` 開頭三條規則（複用既有 material type 必先 Read 完整分支 / material type 命名走物件語義 / debug artifact 先 Read 該 hitType 分支）。
